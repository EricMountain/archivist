{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "types": ["node", "aws-lambda"],
    "paths": {
      "@archivist/core/*": ["../core/*.ts"]
    }
  },
  "include": ["**/*.ts"]
}
