# Implementation status

Tracks progress against [01-aws-backend.md](01-aws-backend.md) and
[02-android-mvp.md](02-android-mvp.md), step by step. This file is the source of truth
for "is X done" — don't infer it from memory of a past session, and don't infer it from
a file's mere existence either: a file can exist and still not meet its step's "Done
when". See "Keeping this file honest" in [README.md](README.md) for the rule that keeps
it that way.

Status values:

- **done** — meets the step's "Done when" criterion. Verified, not assumed.
- **partial** — some of the step exists. The note says exactly what's missing.
- **blocked** — can't proceed until something else happens (usually: a dependency
  further up this table, or a human prerequisite from README.md).
- **not started**

## Plan 01 — AWS backend

| Step | Status | Notes |
| --- | --- | --- |
| 1.1 Repo scaffolding | done | `make test` runs typecheck + vitest clean. |
| 1.2 Key builders and item types | done | `test/keys.test.ts` — 22/22 pass, byte-equal against `sample-data.md`. |
| 1.3 DynamoDB access layer | done | Ran the DynamoDB-Local + MinIO gated suite this pass (`DYNAMODB_ENDPOINT`/`S3_ENDPOINT` etc. set): 47/47 pass across `test/keys.test.ts`, `test/repo/{repo,repo2,repo3}.test.ts`, `test/lambda/{s3event,keys,uploads}.test.ts` — create asset, attach rendition, timeline pagination + cursor, facet query, trash/restore, rename, primaryRend re-election, purge (S3 + DynamoDB), account deletion, key-wrap invariants, master-key allocation, hash secret, blocked-attempt tracking. |
| 1.4 Cognito user pool | partial | **Deployed live** 2026-08-26 (`terraform apply` to the operator's real `prod` instance). Confirmed via `aws cognito-idp describe-user-pool`: `Policies.SignInPolicy.AllowedFirstAuthFactors = [WEB_AUTHN, PASSWORD]`, `MfaConfiguration: OFF`, `UsernameAttributes: [email]`. `WebAuthnConfiguration` doesn't appear in that API's response at all (not even null vs. absent — the field seems to not be surfaced by `DescribeUserPool` in this AWS CLI/API version); indirectly confirmed instead via `terraform plan` reporting zero drift after apply, which reads the resource back through the provider's own (different) code path. **Not verified**: the step's literal "Done when" — a passkey registered and used to obtain a JWT — needs an interactive WebAuthn ceremony (a real browser + authenticator), which nothing in this session can perform headlessly. |
| — | **security fix, same day** | **`terraform/cognito.tf` shipped with self-service sign-up left on** (Cognito's default — `admin_create_user_config` was never set). Since the discovery document publishes the pool ID and client ID unauthenticated by design, this meant anyone who found the domain could call Cognito's public `SignUp` API directly and `POST /session/bootstrap` would mint them a fully-isolated library — not a read of the operator's data, but unauthorized resource provisioning against the operator's AWS bill, and a direct contradiction of `deployment.md`'s invite-only model. Caught by the user asking how bootstrap was actually gated, not by anything in this session's own review. Checked `aws cognito-idp list-users` before fixing: zero users existed, so the live exposure window (deploy → fix, a few hours) wasn't exploited. Fixed: `admin_create_user_config.allow_admin_create_user_only = true`, applied live, confirmed via `describe-user-pool` (`true`) and a zero-drift `terraform plan`. Step 1.4's plan text and `deployment.md` (new "Inviting someone" section, `admin-create-user`) updated so a future deployment doesn't reintroduce this. |
| 1.5 API Gateway and API Lambda | done | **Deployed live.** The API Gateway's own invoke URL's `/health` → `200 {"status":"ok"}`; the same URL's `/photos` (no auth header) → `401`. Both also verified through CloudFront at `https://photos.example.com/api/health` → `200`. |
| 1.6 Authorisation middleware | partial | `src/lambda/api/auth.ts` written and live (the 401s above prove the JWT authorizer is wired and rejecting unauthenticated calls). Every lambda-level *test* this pass still constructs `req.auth` directly rather than going through `resolveAuth`, so `resolveAuth`'s own JWT→ownerId resolution is unexercised by any test. The cross-owner-read test the step specifically requires needs two real signed-in identities — blocked on the same interactive passkey ceremony as 1.4. |
| 1.7 First sign-in bootstrap | done | `test/repo/repo2.test.ts`'s bootstrap test (run this pass): two calls for the same issuer/subject return the same userId/ownerId and only the first reports `created: true`. |
| 1.8 Key wrapping endpoints | done | Reconciled 2026-08-26: `POST /keys` no longer accepts `masterKeyVer` (server derives it from `#SETTINGS` via `getCurrentMasterKeyVersion`, added `rotatedAt` to `OwnerSettingsItem`/`design.md`/`sample-data.md` so it doesn't need echoing from the client either); added `POST /keys/version` (`allocateMasterKeyVer`, atomic `ADD`) and `PUT /keys/hash-secret` (`putHashSecret`), wired into `router.ts` and `terraform/api.tf`. Verified against DynamoDB Local: `test/lambda/keys.test.ts` — masterKeyVer field is ignored even when present, enrolment after allocation, **two concurrent `POST /keys/version` calls via `Promise.all` return `mk-1`/`mk-2`** (the specific race the step's "Done when" calls out), hash-secret round-trip. `test/repo/repo2.test.ts`'s existing wrap-invariant coverage (enrol two devices, delete-to-one rejected) still passes. **2026-08-27 addition**: `wrapAlg` closed set extended to include `ECDH-ES+AES-KW` (`items.ts`, `keys.ts`), with a new `epk` field required and validated when that `wrapAlg` is used — see 2.4a below for why. Verified against DynamoDB Local: new case in `test/lambda/keys.test.ts` (missing `epk` rejected, present `epk` accepted, 48/48 total passing). **2026-08-30 addition**: `GET /keys/hash-secret` (`getKeyHashSecret`, reuses `getOwnerSettings`), closing design.md open question 4 — found blocking plan step 2.7, not just 2.10, since the *same* device also has no way to recover its own hash secret after the enrolment session that generated it ends. Wired into `router.ts`/`terraform/api.tf`/`api.md`. Verified against DynamoDB Local (spun up a throwaway DynamoDB Local + MinIO, no committed compose file, torn down after — see 1.8's earlier addition for the same pattern): 2 new cases in `test/lambda/keys.test.ts` (404 before any `PUT`, exact round-trip after), 56/56 across the full gated suite, `npm run typecheck` clean. |
| 1.9 Upload API | partial | `routes/uploads.ts` covers the documented sequence. Added this pass: the hash-check now distinguishes a live-but-trashed hit (record a blocked attempt, or restore if `reAddDeleted`) from an ordinary live duplicate, and a tombstone hit now records `blockedAttempts`/`lastAttemptAt`/`lastAttemptBy` and pushes `expiresAt` forward — verified in `test/lambda/uploads.test.ts` against DynamoDB Local + MinIO (all three hash-check branches, S3 objects included). **Not verified**: the literal "IMG_1.CR3 then IMG_1.JPG → one asset, JPEG primary" and "concurrent uploads of both produce the same result" scenarios via `postUpload` itself — that grouping logic is only verified at the `repo/ingest.ts` layer (`test/repo/repo.test.ts`), not through the HTTP route end to end. **2026-08-30 additions, found while implementing plan step 2.10**: (1) `POST /uploads` now accepts an optional client-minted `photoId` (validated as a ULID), used when creating a new asset; the response gained `created`/`encDek`/`encKeyId` so the client can tell whether its own candidate survived — see "Why the client gets to propose a photoId" in design.md. (2) `encIv` was wrongly required unconditionally; fixed to be required only when `encChunkSize === 0` (whole-object mode), matching `crypto-format.md` and `RenditionItem.encIv`'s own optional typing — streaming-mode uploads (>32 MiB) would have been rejected outright before this. (3) A live hash-duplicate whose asset is still `status: processing` (its uploader died mid-PUT — a process kill, lost connectivity) now gets re-presigned URLs and a `resumed: true` response (with the rendition's own already-fixed `encIv`/`encChunkSize`, which a resuming client must reuse bit-for-bit) instead of a bare `duplicate: true`, which carried no way to ever finish it — see "Resuming an interrupted upload" in design.md. All three verified against DynamoDB Local + MinIO: `test/lambda/uploads.test.ts` grew from 3 to 10 cases (candidate-photoId create/attach, ULID rejection, streaming-mode-no-encIv accepted, whole-object-mode-no-encIv still rejected, resume re-presigns with the original encIv/encChunkSize, a `ready` asset still gets the bare duplicate). Full suite 63/63, `npm run typecheck` clean. **Known gap surfaced, not fixed**: `attachAndRespond` still never updates `#META.thumbs`/`exifEnc` even when the attaching rendition becomes primary (`becomesPrimary`) — pre-existing, not introduced this pass; 2.10's Android worker works around it by never uploading thumbnails on a plain attach (see its own row). |
| 1.10 S3 event handler | done | `test/lambda/s3event.test.ts` run against DynamoDB Local + MinIO this pass: status stays `processing` until every declared object (original + each thumb) exists with the declared size, flips to `ready` once they all do, and flips to `failed` on a size mismatch. |
| 1.11 Read endpoints | partial | `routes/photos.ts`, `routes/facets.ts` wired into `router.ts`. Not independently verified — no test exercises `GET /photos`, `/photos/{id}`, `/facets`, or `/facets/{type}/{value}` directly (this route-handler-level gap is unchanged by the fix below). **Fixed 2026-08-30** (found while implementing plan step 2.6, fixed same day): `dto.ts`'s `timelineEntryDto`/`facetEntryDto` never returned `takenAt` — the only fields recovered from the GSI key were `photoId`/`facetType`/`facetValue`, missing the same treatment for the timestamp embedded in `timelineSk`/`facetSk`. Added `parseSortKey` to `keys.ts` (inverse of `sortKey`, same file/section as `photoIdFromMediaPk`/`parseFacetGsiPk`) and used it in both DTO functions. New `test/dto.test.ts` (3 tests, direct unit tests of the previously-untested DTO layer) plus 3 new `parseSortKey` cases in `test/keys.test.ts` (25/25 passing, up from 22) — round-trip, the A6/A7 same-timestamp tiebreak case, and a facet value containing `#`. `npm run typecheck` clean. **Not run this pass**: the DynamoDB-Local/MinIO-gated suite (`test/repo/*`, `test/lambda/*`) — unaffected by this change in principle (pure functions, no repo/lambda logic touched) but not re-confirmed live. |
| 1.12 Mutations | partial | Rename/delete/restore verified at the repo layer (`test/repo/repo2.test.ts`: rename moves the PATH pointer, primaryRend re-election, last-rendition trashes the asset). Added this pass: `GET /trash` now enriches each entry with its primary rendition's `blockedAttempts`/`lastAttemptAt`/`lastAttemptBy` when non-zero — verified in `test/lambda/uploads.test.ts`. **Not verified**: `PATCH .../renditions/{rid}`, `DELETE /photos/{id}`, `DELETE .../renditions/{rid}`, `POST .../restore` through their actual route handlers (only through the repo functions they call). |
| 1.13 Purge sweep | done | Fixed this pass: `purgeAsset` now takes `tombstoneRetentionDays` and sets `expiresAt` (epoch seconds, via the new `epochSecondsAfterDays` helper) on every tombstone conversion, without touching pre-existing `blockedAttempts`/`lastAttemptAt`/`lastAttemptBy`; `src/lambda/purge/index.ts` reads `tombstoneRetentionDays` from owner settings (default 365) and threads it through. `terraform/dynamodb.tf` now has `ttl { attribute_name = "expiresAt" enabled = true }`. Verified: `test/repo/repo3.test.ts` confirms `expiresAt` is set on purge and pushed further out by a later blocked attempt; `terraform validate`/`fmt` clean. **Not verified**: TTL actually expiring an item on a real table (DynamoDB Local doesn't emulate TTL expiry; needs a live deployment and a wait). |
| 1.14 CloudFront, certificate, discovery | done | **Deployed live.** `curl https://photos.example.com/.well-known/archivist.json` → `200`, valid ACM cert (`SSL certificate verify ok`, issued by Amazon, matches the domain), correct body (`apiBase`, `region`, `cognito.userPoolId`/`clientId`, `cryptoVersion: 1`, `instanceName`). Direct S3 (`https://archivist-originals-....s3.eu-west-1.amazonaws.com/...` and the web bucket) → `403`. `/media/*`, `/thumbs/*`, `/api/*` all resolve through their CloudFront Functions and origins without routing errors. **2026-08-30 addition**: `wellknown.tf` also serves `.well-known/assetlinks.json` now (new `passkey_cert_fingerprints` variable, `map(list(string))` keyed `debug`/`release`, empty by default). Written on the theory that this was needed for plan step 2.4's Credential Manager ceremony — **that theory didn't hold up**: a live test the same day found passkey creation succeeding both before and after this was deployed, on the same emulator/account. Kept anyway (correct per Google's spec, costs nothing), but see design.md open question 2 for the full account — it's an open question, not a confirmed fix. Deployed to `dev` and confirmed serving correctly: `curl -i https://<dev-domain>/.well-known/assetlinks.json` → `200`, `content-type: application/json`, correct body, no redirect. Not yet applied to `prod` (no release fingerprint exists to put there yet — Play App Signing hasn't happened). |
| 1.15 Account deletion | partial | `routes/account.ts` written and wired. `deleteOwnerData` verified at the repo layer this pass (`test/repo/repo2.test.ts`: removes media, a pre-existing purge tombstone, S3 objects, and the owner-registry row). The route handler itself (confirmation-token check, Cognito `AdminDeleteUser` call) is not independently verified — would need a real signed-in user to delete, which nobody should do against the live `prod` instance just to test it. |
| 1.16 Crypto format spec and conformance vectors | done | `docs/design/crypto-format.md` is the spec. `tools/gen-vectors/generate.py` produces all 23 required cases into `testdata/vectors/` (was 22 — case 23, ECDH-ES+AES-KW, added 2026-08-27 when Android's device-wrap route changed; see 2.4a below); every case self-verified at generation time. |

**Deployment status, as of 2026-08-26:** `terraform apply` run against the operator's
real `prod` instance (see `private/instance/` for the actual account/region/domain)
using the operator's own AWS profile, at the user's explicit direction — 65 resources
added, 1 changed (DynamoDB TTL
enabled in-place), 0 destroyed, no errors. A follow-up `terraform plan` reports zero
drift. Live-verified this pass: `GET /health` (direct and
via CloudFront), unauthenticated 401s, the discovery document over a valid cert, and
direct-S3 403s on all three buckets. **Not live-verified:** anything requiring a signed-in
user (passkey enrolment, uploads, cross-owner isolation, account deletion) — all of it
needs an interactive WebAuthn ceremony this session cannot perform headlessly. The table
above marks each step's status independently of this note; read both.

**A `dev` instance now exists (2026-08-29),** specifically to unblock the interactive
verification above without touching the real library — see "Dev instance" below for how
it's deployed and reached.

## Dev instance

A second, throwaway deployment for testing, at `archivist-dev.<the operator's domain>`
(exact hostname in `private/instance/dev.tfvars`). Same AWS account and region as `prod`,
same Terraform config, isolated by a separate Terraform workspace (`dev`) and
`environment = "dev"` — separate DynamoDB table, S3 buckets, Cognito pool, Lambdas and
CloudFront distribution; see "Running a second environment" in `terraform/README.md` for
the workflow (`make plan-dev` / `make deploy-dev`).

Deployed 2026-08-29: `terraform apply -var-file=../private/instance/dev.tfvars` against
the `dev` workspace — 79 resources added, 0 changed, 0 destroyed. One transient error on
first apply (`aws_s3_bucket_notification.derived`: `PutBucketNotificationConfiguration`
`InvalidArgument`, S3 validating the Lambda invoke permission before its IAM propagation
caught up) — a plain re-run of the same apply succeeded (1 added) with no config change
needed; if this recurs, it's this known race, not a bug. Live-verified: discovery
document and `/api/health` both `200` through CloudFront on the real domain;
`admin_create_user_config.allow_admin_create_user_only = true` and zero users on the new
pool (confirmed via `describe-user-pool`/`list-users` — the self-service-sign-up gap
that hit `prod` did not reoccur here, since the fix is already in `cognito.tf`).

Not yet done: no passkey has been enrolled against it, so it's ready for but hasn't yet
been used for the interactive verification plan 01/02 steps below still need.

## Plan 02 — Android MVP

Depended on plan 01 being deployed and 1.16's vectors existing — both are now true (see
the deployment note above). A `dev` instance now exists (see "Dev instance" above) for
interactive testing that would otherwise risk the operator's real `prod` instance.

| Step | Status | Notes |
| --- | --- | --- |
| 2.1 Project scaffolding | done | `./gradlew assembleDebug` produces a debug APK. |
| 2.2 Crypto module | done | `:core:crypto:testDebugUnitTest` — all 22 conformance vectors pass, plus a 100 MB streaming round-trip and a truncated-ciphertext-fails test. |
| 2.3 Instance connection | done | `data/remote/{DiscoveryDocument,DiscoveryApi,DiscoveryClient,NetworkModule}.kt`, `data/local/{InstanceStore,LocalStorageModule}.kt`, `data/repo/InstanceRepository.kt`, `ui/onboarding/{ConnectViewModel,ConnectScreen}.kt` written; `MainActivity` routes on connection state. HTTPS-only host input (bare host or explicit `https://`; `http://` and any other scheme rejected outright, no fallback); distinguishes `InvalidHost`/`HostNotFound`/`NotArchivist`/`ServerTooNew` per the plan's error-mode requirement; persists per-host to DataStore (map keyed by host + a "current" pointer, not a singleton). 22 unit tests pass (`:app:testDebugUnitTest`): `DiscoveryClientTest` (host normalization, all four error modes, cryptoVersion boundary) against a fake `DiscoveryApi`; `DiscoveryApiWireFormatTest` against a real MockWebServer, confirming the actual Retrofit/kotlinx.serialization stack throws `HttpException`/`SerializationException` where `DiscoveryClient` assumes it does; `InstanceStoreTest` (DataStore round-trip via a temp-file store, no Robolectric needed); `ConnectViewModelTest` (state machine, using a real `InstanceRepository`/`InstanceStore` backed by a temp-file DataStore sharing the test's `StandardTestDispatcher` scheduler — the naive version of this test was flaky/deadlocked because DataStore's default internal scope is real `Dispatchers.IO`, invisible to a test's virtual scheduler). `DiscoveryDocument`'s fields checked by hand against `terraform/wellknown.tf`'s real `jsonencode(...)` body — exact match. Introduced JUnit5 (Jupiter) for this module rather than the crypto module's JUnit4, matching the target test stack in `docs/design/android.md`; the two modules now use different test runners on purpose (`useJUnitPlatform()` set on `:app` only). **2026-08-29 addition**: `ConnectViewModel.changeInstance()` lets a `Connected` instance back out to `NeedsConnection` (prefilled with the host being left) without touching what's stored — nothing is overwritten until a new `connect()` actually succeeds. Two new `ConnectViewModelTest` cases cover it (backs out with the right prefill; no-op when not connected). **All three "Done when" clauses now confirmed live, closing this step out**: (1) connecting to the `dev` instance persists its config — it's the instance used for every emulator test since (see 2.5's live pass); (2) a typo'd hostname → `HostNotFound`, confirmed 2026-08-27; (3) **confirmed 2026-08-30**: on a freshly-cleared Pixel 8a emulator (app data/cache removed, app restarted — the other emulator, a Pixel 9a, was left alone), entering `google.com` as the host was correctly diagnosed as reachable but not an Archivist server (`NotArchivist`), distinct from `HostNotFound`. All three error/success modes the step's "Done when" requires are now distinguished on real (if emulated) hardware, not just in unit tests. |
| 2.4 Authentication | done | `data/remote/{CognitoAuthApi,CognitoAuthClient,CognitoNetworkModule,ArchivistApi,ArchivistApiFactory}.kt`, `data/local/{TokenStore,LocalStorageModule addition}.kt`, `data/repo/AuthRepository.kt`, `ui/onboarding/{SignInViewModel,SignInScreen,PasskeyCeremony}.kt` written and wired into `MainActivity` after a successful connection. Scope grew beyond the plan's literal "sign in with a passkey": an invited account has no registered passkey and no hosted UI to bridge that gap, so the app also handles the temporary-password → `NEW_PASSWORD_REQUIRED` → passkey-registration path — see the new note under "Auth and key unlock" in `android.md`. Cognito's user API confirmed live to be plain unsigned HTTPS JSON-RPC (`X-Amz-Target` header) — no AWS SDK, no SigV4 — and that `application/json` silently fails (200 with `UnknownOperationException` in the body, ignoring `X-Amz-Target` entirely) where `application/x-amz-json-1.1` is required; also confirmed live: requesting a `WEB_AUTHN` challenge for an account with no registered passkey returns `ChallengeName: SELECT_CHALLENGE`, not an error. 64 unit tests pass (`:app:testDebugUnitTest`, up from 22 in 2.3), covering the Cognito wire client against a fake API, the token store, and — the part that couldn't be checked live — the OkHttp `Authenticator`'s refresh-on-401-exactly-once logic, verified against a real MockWebServer (attaches the stored token; refreshes and retries once on 401; never retries a second time even if the retry also 401s; a failed refresh clears the session and gives up). Caught and fixed two real bugs this pass: a `parseToJsonElement` call that threw instead of returning a typed failure for malformed credential JSON, and a StandardTestDispatcher/real-network race in the ViewModel tests (same class of issue as 2.3's DataStore one, this time from OkHttp's real thread pool — fixed with a bounded real-time poll rather than `advanceUntilIdle()` alone). **2026-08-30: the Credential Manager ceremony ran live for the first time, and succeeded.** On a Pixel 8a emulator (app data/cache cleared, fresh install) against the `dev` instance: signed in with the account's existing password, `PasskeyCeremony.register()` triggered a real system dialog ("create a passkey on another device?"), completed successfully, then proceeded into 2.5's recovery-code enrolment and reached "Unlocked". First-ever live confirmation that `PasskeyCeremony.kt` actually works against a real Credential Manager provider, not just the documented API surface. **Caveat worth being precise about**: this same ceremony had *already* succeeded on an earlier run on the same emulator/account, before `.well-known/assetlinks.json` existed at all (see 1.14's row, design.md open question 2) — so whatever was previously assumed to be blocking this (a missing Digital Asset Links file) evidently wasn't, at least on this emulator. **Restart persistence also confirmed live, same session**: force-stopping and relaunching the app, twice, never re-prompted for sign-in (password/passkey) either time — the stored Cognito session survived both restarts. (One restart also showed the device-PIN confirmation screen, the other didn't; that's `NeedsDeviceUnlock`, plan step 2.5's Keystore auth-window mechanism — driven by "has this device been unlocked in the last 5 minutes," not by anything in 2.4 — not a re-login prompt, and its non-determinism here is exactly the documented behavior, not a bug.) **Refresh confirmed live too, several hours later**: relaunching the app after several hours — well past the 1-hour access token TTL (`access_token_validity = 1`, `terraform/cognito.tf`) — again showed the device-PIN screen (2.5's `NeedsDeviceUnlock`, not a login prompt) and reached "Unlocked" directly, with no password/passkey re-entry. Since the stored access token from the original sign-in was certainly expired by then, this is the first live confirmation that the OkHttp `Authenticator`'s refresh-on-401 path (unit-tested against MockWebServer since this step's first pass, never before exercised against the real backend) actually works end to end. **All three of this step's "Done when" clauses are now confirmed live — closing this step out**: (1) a passkey registered on `dev` signs in — confirmed 2026-08-30 (real Credential Manager ceremony, Pixel 8a); (2) survives an app restart — confirmed, multiple restarts, no re-login; (3) refreshes without re-prompting — confirmed as above. **2026-08-29 addition**: the username-entry step now has a "Change server" action wired to `ConnectViewModel.changeInstance()`, so a wrong-instance sign-in (e.g. picked prod, meant dev) doesn't dead-end — previously there was no way back to `ConnectScreen` from anywhere past it. |
| 2.4a Keystore algorithm spike | done | Run for real on a physical device (Motorola Edge 20 Lite, Android 13/API 33, no StrongBox chip — user-supplied, connected via USB, deliberately the oldest device available rather than a newer one that wouldn't answer whether the oldest works). Results: RSA-3072/2048 keygen works in the TEE (~1.3s/~150–300ms) but refuses StrongBox (no chip); **RSA-OAEP-256 decrypt via the Keystore-resident private key fails outright** (`InvalidAlgorithmParameterException: Unsupported MGF1 digest: SHA-256. Only SHA-1 supported`) even though encrypt via the same key works fine — isolated to decrypt specifically by splitting the check into three probes across two runs, since the first run's stack trace was initially misread; EC-P256 keygen works but lands in software, not the TEE; ECDH agreement against a Keystore-resident EC-P256 key **works correctly end to end**, confirmed from both agreement directions. Verified live that `KeyAgreement.getInstance("ECDH")` (no explicit provider) auto-routes to the Keystore implementation, same as `Cipher.getInstance` already does for RSA — confirmed before relying on it in `EcdhEs.kt`. Open question 1 in `design.md` is resolved: Android device wrapping moves to `ECDH-ES+AES-KW`. `KeystoreSpike.kt` deleted per the plan's own instruction, results recorded in `design.md`/`crypto-format.md` instead. **Still open**: whether the software-not-TEE result for EC-P256 is this device's Keymaster specifically or broader — not re-tested on other hardware this session (see design.md open question 1's remainder). |
| 2.5 Key enrolment and recovery code | done | `KeyCustody.kt`/`DeviceKeystore.kt` (`:core:crypto`); `EnrolmentRepository.kt`/`EnrolmentViewModel.kt`/`EnrolmentScreen.kt`, `data/local/EnrolmentStore.kt`, `data/repo/MasterKeyHolder.kt` (`:app`); wired into `MainActivity` after sign-in and into `ArchivistApplication.onTrimMemory`. Covered by **94 passing JVM unit tests across both modules** (`:core:crypto` 10, `:app` 84). **All three "Done when" scenarios confirmed live against the `dev` instance on 2026-08-30** (two Android Studio emulators; exact AVD/API not recorded) — the first time any part of 2.5 has run against a real deployed backend rather than a fake/local one: (1) fresh install enrolled, restarted, and unlocked silently; (2) a second, separate emulator recovered the master key using only the recovery code and reached "Unlocked" (after a crash on this exact path was found and fixed mid-session, see "Last audit" — `NoSecureLockScreenException`); (3) clearing that same second emulator's PIN and relaunching produced the recovery-code re-enrolment prompt, not a crash — `KeyPermanentlyInvalidatedException` handling confirmed live for the first time. Re-entering the recovery code with no PIN still set correctly failed with the new clean error (expected — re-enrolling still needs a working Keystore, which needs a lock screen) rather than crashing again. Two real product-level gaps remain, written up as open questions 3 and 4 in `design.md` rather than guessed around: (3) `PURPOSE_AGREE_KEY` needs API 31 but `minSdk` is 28, so there is no working hardware-backed device-wrap route at all below that; (4) no endpoint lets a later device fetch the owner's wrapped `hashSecret`, which plan step 2.10 will need. **Beyond the step's literal "Done when", still unverified**: a real biometric (as opposed to PIN/pattern/password) satisfying the auth window; the `NeedsDeviceUnlock` UI path's `KeyguardManager.createConfirmDeviceCredentialIntent` hand-off; whether changing to a *different* PIN (as opposed to clearing it) also invalidates the key on this platform/API level — only the clear-PIN case was tested, and it's the one Android's docs unambiguously document, so this doesn't block "done" but is worth knowing; and `finishFirstEnrolment()`'s retry-after-partial-failure non-idempotency. **2026-09-01 addition, found while implementing plan step 2.11's offline "Done when" clause**: `trySilentUnlock` fetched this device's wrapped key material fresh from the server on *every* launch with no fallback, so a cold start with no network could never reach `Unlocked` at all, regardless of how well-cached everything past it was. `EnrolmentStore` gained `cachedDeviceWrap`/`saveCachedDeviceWrap`/`clearCachedDeviceWrap`; `trySilentUnlock` now falls back to that cache on `IOException`. See 2.11's own row and this file's "Last audit" for the full account, including the live offline confirmation. Doesn't change this step's own "Done when" (none of its three clauses mention offline), so status stays `done`, not reopened. |
| 2.6 Local storage | done | `data/local/db/{PhotoEntity,RenditionEntity,UploadQueueEntity,LocalTombstoneEntity,SyncStateEntities,Converters,AppDatabase}.kt`, wired into `LocalStorageModule.kt`. Six tables: `photos` (timeline cache mirroring `timeline_gsi`'s own projection, plus `takenAt` denormalized from `timelineSk` since Room can't split a compound sort key), `renditions` (FK-cascaded to `photos`, `contentHash` indexed for 2.7's dedup check), `upload_queue`, `local_tombstones`, and `sync_state`'s two kinds of state split into their own tables (`sync_state` for folder selections, `timeline_cursor` for the `RemoteMediator` cursor) since the plan's one name covers two unrelated shapes — see the KDoc on `SyncStateEntities.kt`. Room 2.8.4 + KSP; `exportSchema = true` (`ksp { arg("room.schemaLocation", ...) }`), schema JSON present at `android/app/schemas/fr.enry.archivist.data.local.db.AppDatabase/1.json`. **Verified**: 20 new DAO tests (round-trip incl. nested `thumbs` JSON, upsert-replaces-not-duplicates, timeline ordering incl. same-instant tiebreak matching sample-data.md's A6/A7, FK cascade delete, dedup/tombstone lookups, folder enable/disable without deleting the row, cursor replace-not-append) run as plain JVM unit tests against an in-memory DB (`androidx.sqlite:sqlite-bundled-jvm` + a mocked `Context`, no Robolectric/device — see `TestDatabase.kt`'s note on why the mock is safe here); `:app:testDebugUnitTest` 104/104 (up from 84), `:core:crypto:testDebugUnitTest` 10/10 unchanged, `./gradlew assembleDebug` builds. **Found and worked around a real Room/driver bug** (`@Upsert` silently unreliable under test) — see `android/AGENTS.md` for the full explanation and the fix pattern used across these DAOs; not repeated here since it's toolchain knowledge, not step status. **Not yet exercised**: nothing has run against a real Android SQLite implementation (device/emulator) — only the JVM/bundled-driver path. **Separately discovered gap, fixed same day**: `dto.ts`'s `timelineEntryDto`/`facetEntryDto` didn't return `takenAt` at all, which plan step 2.11 will need — see step 1.11's row above for the fix. |
| 2.7 Folder selection and scanning | partial | `sync/{MediaStoreSource,AndroidMediaStoreSource,SyncModule,Scanner}.kt`, `ui/settings/{FoldersViewModel,FoldersScreen}.kt`. Manifest gained `READ_MEDIA_IMAGES`/`READ_MEDIA_VIDEO` (API 33+) and `READ_EXTERNAL_STORAGE` (`maxSdkVersion=32`, for API 28–32 — the plan text only names the two API-33+ permissions, this fills the gap down to `minSdk`). `FoldersScreen` owns the rationale-then-request permission ceremony itself (the one screen in this app that legitimately needs platform Context, unlike the rest which are pure state+callback); not yet wired into any navigation, since nothing hosts a Settings destination until plan step 2.14 — this screen is complete and tested on its own ahead of that. `Scanner.scan()`: for each enabled folder's MediaStore files, skip anything `UploadQueueDao.getByLocalUri` already knows about, else read the whole file once computing `ContentHash.of(hashSecret, ...)` (`:core:crypto`, already existed), then `PENDING` (new candidate) / `DONE` (tombstoned or already-uploaded, recorded anyway so a future scan's `getByLocalUri` check still skips re-hashing it) via a new `insertIfNewContent` (`@Insert(onConflict = IGNORE)` — two different local files with identical bytes is a real scenario, e.g. an auto-backup app duplicating into a second folder; must check the returned row id for `-1`, Room/SQLite's signal that IGNORE dropped it, or a naive queued-count goes stale). `FoldersViewModel.setFolderEnabled` triggers a scan immediately on enabling (not disabling) — "selecting a folder queues its unsynced files" reads as an immediate consequence in the plan's own wording, not something waiting on a separate manual action. **Found and fixed a real gap while implementing this**: 2.7 needs `ContentHash.of(hashSecret, ...)`, and nothing let any device — not just a second one — actually obtain `hashSecret` after the enrolment session that generated it ended (design.md open question 4, previously scoped to "blocks 2.10" only). Added `GET /keys/hash-secret` (backend, see 1.8's row) and the Android side: `HashSecretHolder` (mirrors `MasterKeyHolder`, memory-only, cleared alongside it in `ArchivistApplication.onTrimMemory`) and `EnrolmentRepository.ensureHashSecret()` (cached fast path; the first device populates it for free from what `beginFirstEnrolment()` already generated, no network round trip; a later/recovered device fetches and unwraps it). 4 new `EnrolmentRepositoryTest` cases. **Verified**: 9 `ScannerTest` cases (selecting queues unsynced files; an unselected folder is never touched; deselecting stops future uploads without touching what's already queued; a tombstoned file is never queued `PENDING`; a file already backed up under a different hash record isn't re-queued; a re-scan never re-hashes a file it's already seen; two identical files in different folders are queued once, not rejected, with the known-and-documented limitation that the second URI isn't itself remembered by the fast path, so it gets re-hashed — and again recognised as a duplicate — on a future scan; a scan with nothing selected is a no-op, no crash; failing cleanly with no hash secret available) plus 4 `FoldersViewModelTest` cases, all run as plain JVM unit tests. `:app:testDebugUnitTest` 121/121 (up from 104), `:core:crypto:testDebugUnitTest` 10/10 unchanged, `./gradlew assembleDebug` builds. Found and fixed two real JVM-test-only bugs along the way — both written up in `android/AGENTS.md` rather than repeated here: `ArchTaskExecutor` needing a manual JVM-safe delegate for any Room `Flow` query collected through a `Dispatchers.Main`-resolvable context, and a Room suspend call resuming on a real thread rather than the test's virtual dispatcher (needing the same bounded-poll pattern as the pre-existing `DataStore`/OkHttp entries there). **2026-08-30, live**: the user built and deployed the real debug APK to an emulator — starts cleanly, no crash, confirming this step's code (Scanner, FoldersViewModel/Screen, the manifest permission entries, the new hash-secret plumbing) doesn't break anything already working. **Not verified beyond that**: `FoldersScreen` isn't reachable from anywhere in the running app (see above — pending 2.14's navigation), so none of MediaStore querying, the actual permission dialog, or a real scan have been exercised on a device — only JVM unit tests cover that so far. Status `partial` on both counts. |
| 2.8 Metadata extraction | done | `domain/{Timestamps,ExifExtractor}.kt`. `ExifExtractor` wraps `androidx.exifinterface:exifinterface:1.4.2` (new dependency), reading raw tag strings (`DateTimeOriginal`, `OffsetTimeOriginal`, dimensions, camera make/model/serial, lens) plus `GPSDateStamp`/`GPSTimeStamp` combined into a UTC `Instant` via the library's own `getGpsDateTime()` — deliberately not hand-parsed, since `GPSTimeStamp` is stored as three RATIONALs, not the `HH:mm:ss` string its name suggests. Also provides `deviceKey()` (`<make>\|<model>\|<serial>`, normalised per design.md) and extension-based `mimeFromDisplayName()` (`ExifInterface` has no public MIME getter). `Timestamps.resolve()` is the pure client-side ladder from design.md's "Establishing `takenAt`"/"Resolving the UTC offset at ingest" — every input (including the clock) is a parameter, so it needs no device, backend, or real image file to test. Returns only the two `takenAtSrc` rungs a client can ever produce (`exif`/`file-mtime`; `s3-mtime`/`upload` are server-only per design.md's own note) but all seven `tzSrc` rungs, since design.md is explicit that "the offset ladder runs on the client at ingest." **Verified**: `TimestampsTest` (16 cases) — the four fixtures the step's "Done when" literally requires (EXIF-with-offset, EXIF-with-GPS-only, EXIF-with-neither, no-EXIF-at-all) plus the remaining five rungs (upload-forced beating exif-offset, exif-offset beating upload-fallback, upload-fallback, device-default, owner-`homeTz` via real DST-aware `ZoneId` rules), GPS-delta 15-minute rounding, future/pre-1990 rejection falling back a rung, and both-rungs-implausible returning null. `ExifExtractorTest` (9 cases) runs the real library against real image bytes: a plain `javax.imageio`-generated JPEG for the no-EXIF/no-GPS cases, and a committed fixture (`src/test/resources/exif-fixtures/canon-with-gps.jpg`, 994 bytes) with real EXIF/GPS tags for the positive cases — generated once with Python's Pillow rather than through `ExifInterface`'s own writer, see below. `:app:testDebugUnitTest` 146/146 (up from 121), `:core:crypto:testDebugUnitTest` 10/10 unchanged, `./gradlew :app:assembleDebug` builds. **Found a real environment gotcha, not from inspection**: `androidx.exifinterface.media.ExifInterface`'s static initializer calls `android.util.Log.isLoggable`, which the bare `android.jar` stub throws "not mocked" for — needed a new `testOptions.unitTests.isReturnDefaultValues = true` in `app/build.gradle.kts` (an AGP flag, not Robolectric) to even construct the class in a JVM test. That flag then turned out to have a sharp edge of its own: it also stubs `android.util.Pair`'s constructor (used internally by `ExifInterface.setAttribute()`), leaving both fields null and producing a confusing `NullPointerException` several frames downstream — found by reflectively calling the library's own private `guessDataFormat` and watching it happen directly, not by reasoning about the bytecode (which reads as if it can't return null). Worked around by never writing EXIF tags through `ExifInterface` in this test environment — the committed fixture was generated externally instead. Full account in `android/AGENTS.md`. **Also confirmed empirically rather than assumed**: `getGpsDateTime()` is genuinely `Long?` in 1.4.2 (not a sentinel — a first draft's `Long.takeIf{it>=0}` treatment of it failed to compile), and `TAG_IMAGE_WIDTH`/`TAG_IMAGE_LENGTH` are correctly derived from a JPEG's own SOF marker even with zero EXIF tags present, confirmed against a real `javax.imageio`-generated image with no metadata at all. **Not done as part of this step, by design**: wiring the ladder/extractor into `Scanner`/`UploadQueueEntity` or the actual upload call — the plan scopes that to 2.10 (Upload worker), and `UploadQueueEntity`'s `takenAt`/`tzOffsetMin` columns (added in 2.6) still have no `takenAtSrc`/`tzSrc` columns, which isn't needed until whichever step actually persists a resolved rung. **Not verified**: nothing here ran on a real device or emulator — pure Kotlin domain logic with no Android-framework dependency beyond `ExifInterface` itself, which this pass confirmed is genuinely JVM-safe for reading (not writing), so a device pass would be lower-value than for Room/Keystore-dependent steps, but is still unconfirmed. |
| 2.9 Thumbnails | done | `sync/Thumbnailer.kt`: `Thumbnailer` interface (`AndroidThumbnailer` impl, bound in `SyncModule`) mirrors the `MediaStoreSource` pattern — a fake stands in for 2.10's upload-worker tests, since `ImageDecoder` has no JVM fake. Decodes the source **once**, at the sample size for the *largest* rung (2048), then derives 1024 and 256 from that already-downsampled bitmap via `Bitmap.createScaledBitmap` rather than re-decoding three times. `targetDimensions`/`sampleSizeFor` are pure Android-free functions (aspect-preserving, never-upscale sizing; sample-size divisor that decodes to at least the target without going under) — covered by 8 new `ThumbnailerTest` cases run as plain JVM tests (landscape/portrait/square, exact-match, never-upscale, sample-size boundaries against a real 50 MP sensor's dimensions). `:app:testDebugUnitTest` 154/154 (up from 146), `./gradlew :app:assembleDebug` builds. **Live-verified against a real Pixel 8a emulator (API 37)**, closing this step's "Done when": a throwaway instrumented test (`ThumbnailerInstrumentedTest.kt`, deleted after — same convention as 2.4a's `KeystoreSpike.kt`) generated a real 8688x5792 (~50.3 MP) image via MediaStore, ran it through `AndroidThumbnailer.generate()`, and confirmed all three WebPs came back with correct dimensions, non-empty bytes, and a valid `RIFF...WEBP` header — plus, independently, that the sample size chosen for the 50 MP source decodes to ~2172x1448 (~12 MB as ARGB_8888), well under the ~50 MB ceiling instead of the ~201 MB a full-size decode would need. Passed in 0.84s (`connectedDebugAndroidTest`). **One substitution, noted honestly**: used a JPEG fixture at 50 MP resolution rather than an actual HEIC — Android has no single-call HEIC *encoder* short of the `androidx.heifwriter` library, which nothing in this module depends on, and the code path under test (`ImageDecoder`'s decode/downsample) doesn't branch on container format, only pixel count. WebP quality fixed at 82, chosen as a reasonable default, not derived from a specific target-size requirement in design.md. |
| 2.10 Upload worker | partial | `sync/UploadWorker.kt` (a `@HiltWorker` `CoroutineWorker` — one work item per file, foreground notification, `NetworkType.UNMETERED`/`setRequiresBatteryNotLow` constraints, exponential backoff), `data/repo/UploadRepository.kt` (the actual per-file pipeline, fully unit-testable without WorkManager), plus a small `UploadScheduler` interface (`sync/UploadWorker.kt`) so `FoldersViewModel`/tests never touch real WorkManager directly. `AppDatabase` bumped to version 2 (`upload_queue` gained `plainBytes`/`fileMtimeEpochSec`, filled in by `Scanner` for free at scan time, and `takenAtSrc`/`tzSrc`, filled in by the worker) — `fallbackToDestructiveMigration`, since nothing has shipped version 1 to a real install. New `domain/Ulid.kt` (client-side ULID minting — see 1.9's row above for why) and `domain/ExifBlob.kt` (the concrete, previously-unspecified shape of the `exifEnc` blob: a JSON summary of `ExifData`'s fields, documented in design.md's "Encrypted EXIF"). Wired into `FoldersViewModel`: a successful scan now enqueues every unfinished `upload_queue` row, not just what it just added; `ArchivistApplication.onCreate()` does the same at every app start, to pick back up anything a process death dropped before WorkManager itself persisted the enqueue. New WorkManager/Hilt-work dependencies (`androidx.work:work-runtime-ktx`, `androidx.hilt:hilt-work`/`hilt-compiler` 2.10.0/1.2.0) and `FOREGROUND_SERVICE`/`FOREGROUND_SERVICE_DATA_SYNC`/`POST_NOTIFICATIONS` manifest permissions. **Verified**: 32 new JVM unit tests (`UlidTest` 4, `ExifBlobTest` 4, `UploadRepositoryTest` 8, plus `FoldersViewModelTest`/`UploadQueueDaoTest` updates) — `:app:testDebugUnitTest` 170/170 (up from 154), `:core:crypto:testDebugUnitTest` 10/10 unchanged, `./gradlew :app:assembleDebug` builds. `UploadRepositoryTest` runs the real crypto against a `MockWebServer`, not just checking that a PUT happened: it decrypts what actually got PUT (original + a thumbnail) using the DEK/IV the response implies and asserts the plaintext round-trips, for all three response shapes (`created`/`resumed`/plain-attach) — this caught a real bug before it shipped (see below) — plus the failure-classification (400 → permanent, 500 → retry) and locked-master-key (retry, zero network calls) cases. **Real bug found and fixed by writing these tests, not by inspection**: the plain-attach branch was regenerating a fresh IV for the original rendition instead of reusing the candidate IV already sent in the same `POST /uploads` request — `uploads.ts`'s `buildRendition` records `encIv`/`encChunkSize` straight off *that* request's body regardless of create-vs-attach (only the DEK differs on attach, never the IV/chunk-size, since the rendition item itself is brand new either way), so encrypting under a different IV than what got permanently committed would have produced an undecryptable object. Confirmed the fix is real, not incidental, by reverting it and watching both affected tests fail with `AEADBadTagException`, then restoring it. **Deliberate scope decisions, not gaps found late**: (1) **Not true S3 multipart** — the plan's own "Multipart parts of 8 MiB" text assumes per-part presigned URLs, which `POST /uploads` has never offered (plan 01 §1.9's own spec only ever said "presign PUTs for the original and each thumbnail," singular); implemented as one streaming OkHttp PUT per object instead (still internally the same 1 MiB Tink segments), see android.md's "Upload pipeline" for the fuller account. (2) A plain attach (not `resumed`) never uploads thumbnails, even when this rendition becomes primary — see 1.9's row for why that's currently safe rather than merely conservative. (3) When EXIF has no dimensions *and* the source exceeds the largest thumbnail rung (2048px), `width`/`height` sent to the server under-report (taken from the downscaled thumbnail) — narrow, and only affects images with neither EXIF dimensions nor a JPEG SOF marker `ExifInterface` can derive them from. (4) `takenAt` server-side rungs (`s3-mtime`/`upload`) aren't reachable from this client — when neither client-side rung resolves, it sends `takenAt: now, takenAtSrc: "upload"` directly rather than omitting the field, since `uploads.ts` still requires `takenAt`. (5) No settings screen exists yet (plan step 2.14) to back the constraints, so `NetworkType.UNMETERED`/`setRequiresBatteryNotLow` are hardcoded defaults, and `setRequiresCharging` (the plan's own "optional") is omitted entirely. (6) `POST_NOTIFICATIONS` is declared in the manifest but never requested at runtime — the worker still runs without it, the foreground notification just won't be visible on API 33+ until some later step adds that prompt. **2026-08-31 addition — live instrumented tests, permanent this time (not the throwaway-spike convention 2.4a/2.9 used).** Three new `androidTest` classes (`sync/{Scanner,Thumbnailer,UploadWorker}InstrumentedTest.kt`) plus `TestEntryPoint.kt` (an `@EntryPoint`/`@InstallIn(SingletonComponent::class)` interface that has to live in `app/src/main`, not `androidTest` — Hilt aggregates entry points at the compilation that builds the *real* `SingletonComponent`, confirmed the hard way; see `android/AGENTS.md`'s new "Instrumented tests" section for this and four more real gotchas found getting these running) and `testutil/MediaStoreFixtures.kt` (real on-device JPEGs via `Bitmap`/`ContentResolver`, no `javax.imageio` — that doesn't exist on Android). No `hilt-android-testing`/`HiltTestApplication` — these run inside the real app process and reach its real Hilt singletons via `EntryPointAccessors`, seeding/restoring just the few they touch (a connected instance, an unlocked master key) rather than replacing bindings. Ran for real against a booted Pixel 8a emulator (`Starting 5 tests on Pixel_8a(AVD) - 17` / all passed, via `./gradlew :app:connectedDebugAndroidTest`): `ScannerInstrumentedTest` (real `AndroidMediaStoreSource` + real Room, 2 cases), `ThumbnailerInstrumentedTest` (real `ImageDecoder`, replaces 2.9's deleted spike permanently, 2 cases), and — the significant one — `UploadWorkerInstrumentedTest`: a real WorkManager job, real Hilt-injected `UploadWorker` (foreground notification and all), real MediaStore file, decrypting what actually landed against a `MockWebServer` standing in only for the network boundary. **Found and fixed two real bugs no JVM test could reach, both confirmed live before and after the fix, not assumed from documentation**: (1) `ArchivistApplication` implementing `Configuration.Provider` was not sufficient on its own — `androidx.startup`'s default `WorkManagerInitializer` still ran first (during `attachBaseContext`, before Hilt's field injection into the Application), locking in the reflective default `WorkerFactory` before the on-demand path ever got consulted; `UploadWorker` (a `@HiltWorker` with an `@AssistedInject` constructor, not a reflective one) failed to instantiate as a result. Fixed by removing the default initializer's manifest entry (`tools:node="remove"` on its `<meta-data>`), per WorkManager's own "on-demand initialization" pattern. (2) `setForeground()`'s `FOREGROUND_SERVICE_TYPE_DATA_SYNC` crashed on API 34+ (`IllegalArgumentException: ... not a subset of foregroundServiceType attribute 0x0`) because WorkManager's own manifest doesn't declare a type for `SystemForegroundService` by default, contradicting an earlier session's assumption that it did (1.14's row); fixed with an explicit manifest override. Both are now `AndroidManifest.xml` entries with doc comments explaining why. **A real, regrettable side effect of this pass**: the emulator used had a genuine prior enrolled session on it (from 2.4/2.5/2.9's earlier live-testing), and `connectedDebugAndroidTest` uninstalls the app-under-test when it finishes — standard AGP behavior, not particular to these tests' own code, but not anticipated before running the full suite. That session's local state (and its now-orphaned device-wrap Keystore key) is gone; recovering it needs a fresh install, reconnecting, and re-enrolling (recovery code, if saved, or first-device enrolment otherwise). Flagged to the user immediately on discovery, and written up as its own warning in `android/AGENTS.md` so a future session checks `adb shell pm list packages` for a real install before running this Gradle task again. **Still not verified**: the step's literal "Done when" at the scale/adversity it actually describes — 100 photos, an interrupted process kill or airplane-mode toggle *mid-upload* (`UploadWorkerInstrumentedTest` covers one clean file, not an interruption), and metered-network pausing (WorkManager's own documented constraint behavior, never independently exercised here). Now unblocked by real emulator access and a working instrumented-test harness for a future pass to extend rather than build from scratch. |
| 2.11 Timeline | done | `data/repo/{PhotoRepository,TimelineRemoteMediator}.kt`, `crypto/EncryptedImageFetcher.kt`, `ui/timeline/{TimelineViewModel,TimelineScreen}.kt` written and wired into `MainActivity` in place of the old "Unlocked" placeholder text. `PhotoDao` gained a Room-generated `pagingSource(): PagingSource<Int, PhotoEntity>` (new `androidx.room:room-paging` dependency); `ArchivistApi` gained `getPhotos`/`PhotosPageResponse`/`TimelineEntryDto` (`GET /photos`, reusing `ThumbEntry` directly for the wire shape). `PhotoRepository.timeline()` is a `Pager` with `TimelineRemoteMediator` (network) over `PhotoDao.pagingSource()` (Room) — the `Int` key Paging itself uses is Room's own row offset, decoupled from `GET /photos`'s opaque cursor string, which `TimelineCursorEntity` tracks separately (a `REFRESH` clears and re-fetches page one; `APPEND` resumes from the stored cursor, or reports `endOfPaginationReached` immediately if none exists yet). `EncryptedThumbRef`/`EncryptedImageFetcher` (in `fr.enry.archivist.crypto`, same package as `:core:crypto` — deliberate, so it can call `WholeObjectCipher`/`Aad`/`MasterKey` with no imports) fetches a plain unauthenticated GET against `/thumbs/*` (per api.md, unauthenticated at the CDN layer by design) and decrypts with the in-memory master key before handing Coil the plaintext WebP bytes; registered via `ArchivistApplication` now also implementing `SingletonImageLoader.Factory`, with the disk cache rooted in `noBackupFilesDir` per android.md (holds **decrypted plaintext**, so it must never reach a cloud backup). `TimelineViewModel.locked` reads `MasterKeyHolder.current` continuously and gates the whole screen to a reused `EnrolmentScreen` (its own silent-unlock-first logic) when the key disappears — including *mid-session*, fixing the staleness plan step 2.5's own STATUS.md note flagged (`MainActivity`'s local `unlocked` boolean never re-checked `MasterKeyHolder` after the first unlock; it still doesn't, but `TimelineScreen` now does, which is what actually matters once you can reach it). Date headers use `PagingData.insertSeparators` keyed on `PhotoEntity.localDate()` (`takenAt` + `tzOffsetMin`, not UTC), extracted as a standalone `Flow<PagingData<PhotoEntity>>.toTimelineItems()` specifically so it's testable without Room/Hilt. Grid is `LazyVerticalGrid(GridCells.Adaptive(96.dp))`, not a true justified layout (a scope simplification, not an oversight — a real justified/masonry algorithm is a substantially bigger undertaking the plan text doesn't otherwise call for). **Two real Room/Paging-in-this-JVM-test-environment bugs found and fixed while writing tests, not by inspection** — both written up in full in `android/AGENTS.md`'s "Testing" section: (1) `RoomDatabase.assertNotMainThread()` NPEs against the bare `android.jar` stub; (2) the deeper, real fix underneath that — `androidx.room.withTransaction` doesn't work at all against a `.setDriver(BundledSQLiteDriver())` database (test *or* production would hit the same `SupportSQLiteOpenHelper`-not-configured `IllegalStateException` if production ever used a driver-based database without a compat helper), so `TimelineRemoteMediator` uses `useWriterConnection`/`Transactor.withTransaction` instead — the modern API, works identically against this repo's real framework-backed production database too, not a test-only workaround. **Verified**: 12 new JVM unit tests — `TimelineRemoteMediatorTest` (8: refresh upserts and stores the cursor, refresh-with-no-cursor reaches end-of-pagination and clears the stored cursor, refresh clears stale cached rows before repopulating, append sends the stored cursor, append/prepend with nothing to resume from never make a network call, a 5xx/no-connected-instance is reported as `MediatorResult.Error` rather than thrown) and `TimelineViewModelTest` (4: `localDate()` uses the offset rather than UTC, a single header ahead of two same-day photos, a new header on a day change, two photos on different *UTC* dates but the same *local* day — one of them crossing UTC midnight via a `+120` offset — land under one shared header, exercising the plan's own "photos taken at 9am should group under that day" requirement directly). `:app:testDebugUnitTest` 182/182 (up from 170), `:core:crypto:testDebugUnitTest` 10/10 unchanged, `./gradlew :app:assembleDebug` builds. **Live-verified, narrowly**: installed the built debug APK on the booted `Pixel_8a` emulator and launched it — starts cleanly, no crash, `adb logcat` shows nothing in `AndroidRuntime`/`FATAL`, screenshot confirms `ConnectScreen` renders (this emulator's prior enrolled session was wiped by 2.10's own `connectedDebugAndroidTest` run, per that step's STATUS.md note, so it never reaches `TimelineScreen` at all without re-signing-in and re-enrolling — needs the account's actual recovery code, which this session doesn't have and shouldn't guess at). **2026-09-01 update — the first "Done when" clause is now genuinely confirmed, live, against real data**: built `android/app/src/androidTest/kotlin/fr/enry/archivist/sync/LoadTestInstrumentedTest.kt` (procedurally-generated synthetic JPEGs, real MediaStore inserts, real `Scanner`→`UploadWorker` pipeline against the real `dev` instance — see this file's "Last audit" section for the full account, including a real Terraform drift that blocked it and a real Compose crash it found and fixed) and ran it at 100 then 1,100 photos. **"1,000 photos scroll smoothly" is now confirmed**: stress-scrolled the full 1,100-photo timeline (rapid swipes, both directions) with no crash, after fixing a real duplicate-grid-key crash the scale test surfaced (`TimelineItem.Header` needed a unique anchor photoId, not just its date — see the audit entry). `:app:testDebugUnitTest` now 183/183. **2026-09-01, later — all three "Done when" clauses now confirmed live, closing this step out.** Testing the remaining two surfaced two more real bugs, both found live against the populated `dev` account and both fixed, not just documented:

* **Revoking key access mid-session**: `adb shell am send-trim-memory <pkg> RUNNING_CRITICAL` (the real mechanism `ArchivistApplication.onTrimMemory` reacts to — not a synthetic hook) while the grid was showing, live-verified: the photo grid vanished *immediately*, replaced by the locked flow — never a grid of broken images. Confirms the "Done when" as written. (A slow *full resolution* to a final locked sub-state on one run was traced to the emulator's own network/DNS layer degrading after this session's very heavy load — `ping` showed corrupted packets and the system clock jumping backward — not a bug in `EnrolmentRepository`/`TimelineViewModel`; the app logged nothing because neither path has logging on it, consistent with a silently-hung network call rather than a crash. Resolved itself after restarting the app.)
* **Opening offline showing cached thumbnails — found genuinely broken, on two independent layers, both fixed**:
  1. **The app could never get *past* the unlock screen offline at all.** `EnrolmentRepository.trySilentUnlock` fetched this device's wrapped master-key material (`GET /keys`) fresh from the server on *every single launch*, with no fallback — so a cold start with no network stopped at `EnrolmentUiState.NetworkError` ("Couldn't reach the server") before the Timeline (which was already fully offline-capable) was ever reachable at all. Fixed: `EnrolmentStore` gained `cachedDeviceWrap`/`saveCachedDeviceWrap`/`clearCachedDeviceWrap` (the wrap's `epk`/`wrappedKey` — not secret, same reasoning `deviceWrapId` already documented: useless without this device's own Keystore private key); `trySilentUnlock` now caches that material on every successful fetch and falls back to the cached copy on `IOException`, still reporting `NetworkError` if nothing's cached yet (never unlocked online before). Also clears the cache alongside `clearDeviceWrapId` on a `KeyPermanentlyInvalidatedException`. 3 new `EnrolmentRepositoryTest` cases (caches on success; falls back to the cache when offline; still reports `NetworkError` offline with nothing cached) plus an assertion added to the existing lock-screen-invalidation test.
  2. **Coil's disk cache was never actually being written to at all**, discovered by checking the device filesystem directly after the fix above got the app *to* the Timeline offline and it rendered a completely blank grid (correct date headers from Room, zero thumbnails). Root cause, confirmed by reading Coil 3.3.0's own `EngineInterceptor`/`NetworkFetcher` source: a `SourceFetchResult` only participates in Coil's disk cache when its `ImageSource` is already file-backed with a `diskCacheKey` attached — a plain `ImageSource(BufferedSource, FileSystem)`, which is what `EncryptedImageFetcher` had always returned, is invisible to Coil's cache-write step. **This means the disk cache had never worked at all, at any point in this step's development** — every thumbnail shown all session, online, was being re-fetched and re-decrypted from network on every single load, not served from disk. Fixed: `EncryptedImageFetcher` now reads/writes the `ImageLoader`'s `DiskCache` itself (mirroring `NetworkFetcher`'s own read-snapshot/fetch/write-editor shape) — critically, caching the **decrypted plaintext**, so a cache hit needs neither the network nor the master key, matching what android.md already claimed ("a cache hit never re-fetches or re-decrypts at all") but which was never actually true until now. Confirmed by direct inspection of `no_backup/thumbnail_cache/` on-device: real journal + data files appeared after this fix, sized right for plaintext WebP thumbnails (1-3KB each).
  3. **Live-verified end to end**: network cut (`adb shell svc data disable` + `svc wifi disable` — `svc wifi disable` alone doesn't do it, this AVD has a simulated cellular radio too), app force-stopped and cold-launched with zero connectivity (`ping` confirms "Network is unreachable") — silently unlocked using the cached wrap, and the full timeline rendered with every thumbnail correctly decrypted from disk cache, indistinguishable from the online view. Screenshotted, not just asserted.

`:app:testDebugUnitTest` 186/186 (up from 183). Fixed files: `EnrolmentStore.kt`, `EnrolmentRepository.kt`, `EncryptedImageFetcher.kt`, plus their tests. The 1,100 synthetic load-test photos are still live on the `dev` instance for further testing — `tools/teardown-load-test.mjs`/`make teardown-dev-load-test` (dry-run validated, not yet executed) removes them without leaving `HASH#` tombstones whenever that's wanted. |
| 2.12 Photo detail | partial | `ui/detail/{DetailScreen,DetailViewModel}.kt`, `data/repo/PhotoDetailRepository.kt` (new: `fetchDetail`/`downloadOriginal`), `ArchivistApi.getPhoto` + `PhotoMetaDto`/`RenditionDto`/`PhotoDetailResponse` (new — `GET /photos/{photoId}` returns raw `#META`/`R#` items per `routes/photos.ts`, not `dto.ts` DTOs, so these declare only the subset this screen needs), `PhotoRepository.observeTimeline()` (new). Wired into `TimelineScreen`: tapping a grid cell opens `DetailScreen` (plain local `selectedPhotoId` state, same no-nav-library pattern every other screen transition here uses). Swiping is a `HorizontalPager` over `PhotoRepository.observeTimeline()`'s plain list (Room's own timeline order, minus headers) rather than the grid's `Paging` flow — index navigation over `LazyPagingItems` doesn't compose with `HorizontalPager` once date headers are mixed into the index. Pinch-zoom is a hand-rolled two-pointer-only gesture loop (`awaitEachGesture` + `calculateZoom`/`calculatePan`, consuming only once a second finger joins — see the 2026-09-02 entry below for why not the more obvious `detectTransformGestures`) + `graphicsLayer` (no new dependency; `HorizontalPager` itself was already available transitively via `material3`, no new dependency needed there either). Metadata panel decrypts `exifEnc` on-device (`PhotoDetailRepository.decryptExif`, same DEK/AAD pattern as thumbnails — renditions have no `encDek` of their own, they share `#META`'s) for camera make/model, and marks the date "(approximate)" whenever `takenAtSrc != "exif"` — the step's own literal "Done when" clause. Rendition list renders as `JPEG · RAW`-style labels (`role == "raw"` always shown as "RAW" regardless of specific raw extension; known display extensions get a friendly label, e.g. `jpg` → `JPEG`). "Original on demand only" (never fetched by `fetchDetail` itself): a `View original` button per rendition calls `downloadOriginal`, which GETs the `/media` CloudFront path unauthenticated (same model as `EncryptedImageFetcher`'s `/thumbs` fetch) and decrypts with the asset's own DEK — a RAW/sidecar rendition Android's `BitmapFactory` can't decode shows an explicit "can't be previewed in-app" message rather than a blank/broken image. **Real toolchain bug found and fixed while implementing this, not pre-existing**: `PhotoDetailRepository`'s class-level KDoc originally spelled out the literal paths `` `/media/*` ``/`` `/thumbs/*` ``, and a literal `/*` inside an already-open `/** */` KDoc opens a *nested* Kotlin block comment, which corrupted `:app:kspDebugKotlin`'s (Hilt's) cross-file type resolution for the whole class — `DetailViewModel`'s constructor reported `PhotoDetailRepository` as an unresolved `ERROR parameter type`, even though the ordinary Kotlin compiler compiled the file fine and even though `PhotoDetailRepository` was correctly imported. Found by bisection (stripped the file to a bare constructor, added pieces back until the KDoc paragraph reintroduced the failure), not by inspection. Fixed by rewording the KDoc to avoid the literal `/*` substring; written up in `android/AGENTS.md` so a future KSP "type could not be resolved" failure naming a real, correctly-imported class doesn't get misdiagnosed as a Dagger graph problem. **Verified**: 12 new JVM unit tests — `DetailFormattingTest` (9: date approximate-marker, tz-offset-not-UTC formatting, byte-unit scaling, RAW-role labelling regardless of extension, known/unknown extension labels, primary-vs-largest-rendition size fallback) and `PhotoDetailRepositoryTest` (3, against a real Retrofit stack via MockWebServer: exif decrypts into camera fields when the master key is available, camera fields stay null with no EXIF at all, `exifDecryptFailed` is reported — not a silent null — when EXIF exists but the key can't decrypt it). `:app:testDebugUnitTest` 198/198 (up from 186), `./gradlew :app:assembleDebug` builds, `:core:crypto:testDebugUnitTest` unaffected. **Not covered by any test, by design, same gap as `EncryptedImageFetcher`**: `downloadOriginal`'s actual network fetch (it hardcodes `https://`, and MockWebServer has no TLS listener without extra certificate bootstrapping this pass didn't set up) — only its decrypt-path logic is exercised indirectly through `fetchDetail`'s tests. **2026-09-02, live-verified with the emulator's device PIN (supplied by the user for this purpose), closing most of this step's own "Done when" — two more real bugs found and fixed in the process, neither caught by the unit suite above:**

1. **Swiping between photos silently did nothing.** `ZoomableThumb`'s `pointerInput` used `detectTransformGestures`, which starts consuming *pan* deltas from a single pointer (it doesn't wait for a second finger before treating a drag as pan) — this swallowed every one-finger swipe before `HorizontalPager` ever saw it. Confirmed live, not from documentation: a real one-finger swipe across the open photo did nothing at all. Fixed by replacing it with a hand-rolled `awaitEachGesture` loop that only reads `calculateZoom()`/`calculatePan()` and calls `consume()` once `event.changes.size >= 2` — an ordinary one-finger swipe now reaches the pager exactly as if the modifier weren't there. Re-verified live: swiping now moves between photos, and the metadata panel updates to match (confirmed date/dimensions/size changed together with the image).
2. **The "Close" button on the "view original" overlay rendered under the status bar**, overlapping the clock/battery icons, because `OriginalOverlay` is a full-bleed `Box(Modifier.fillMaxSize())` that doesn't inherit the `innerPadding` chain the rest of the screen gets from `MainActivity`'s `Scaffold` (unlike the screen's own "← Back", which sits safely below it for free). Taps there landed inconsistently. Fixed with `.statusBarsPadding()` on just the button (the image itself stays full-bleed, ordinary photo-viewer UX) — confirmed live via `uiautomator dump`: the button's bounds moved from `[922,11][1080,116]` (overlapping the status bar, `y` starting at 11px) to `[922,132][1080,237]` (clear of it), and a tap there now reliably dismisses the overlay.

Both fixes rebuilt clean (`:app:assembleDebug`, `:app:testDebugUnitTest` still 198/198) and were reinstalled over the same live session (`adb install -r`, preserving the 1,100 load-test photos and enrolled key from 2.11) rather than losing it.

**What's now confirmed live, end to end, against the real `dev` instance and real uploaded photos** (Pixel 8a emulator, PIN unlock): tapping a grid cell opens `DetailScreen` showing the 2048 thumbnail and decrypted metadata (date, dimensions, size, rendition label); one-finger swiping moves to the adjacent photo and the metadata panel updates correctly (verified going from "1 Sep 2026, 08:39" / 1267×1128 to "31 Aug 2026, 17:08" / 1117×701, a different asset); "View original (JPEG)" fetches and decrypts the full-resolution rendition — confirmed it's genuinely a *different* image from the thumbnail (not just re-showing the cached thumb) by brightness-boosting a screenshot diff, since the 90%-alpha scrim makes the two hard to tell apart by eye at normal brightness; the overlay's "Close" dismisses back to the detail screen; and "← Back" returns cleanly to the grid.

**Pinch-zoom on the paged thumbnail is now confirmed live too**, by the user, not this session's own adb-only testing: the Android Emulator's own mouse-simulated multitouch (on macOS: **Ctrl**+click-drag, not Option as this file's own earlier note here guessed, and not the host trackpad's native pinch gesture, which macOS handles itself and never forwards to the guest as a real multitouch event) drove a real two-pointer gesture and zoomed the photo-detail screen's own image correctly.

**Testing that same pinch-zoom on the "view original" overlay surfaced a third real bug, also found live and also fixed**: pinching over the (visibly frontmost) original image zoomed the *paged thumbnail behind it* instead, and tapping the screen coordinates where "← Back" sits — one level further back, past even the thumbnail — navigated out of the whole screen despite the original still visibly showing on top of everything. Root cause: `OriginalOverlay` was a plain `Box(Modifier.fillMaxSize())` drawn as a second top-level composable alongside `DetailScreen`'s own `Column`, so Compose *drew* it last (on top, hence the correct visual layering) but nothing in it consumed pointer input over its blank/image area, so hit-testing kept walking past it to whatever else occupied that same screen position underneath. Fixed by making `OriginalOverlay` a real `androidx.compose.ui.window.Dialog` (`DialogProperties(usePlatformDefaultWidth = false)` so its content fills the screen instead of the platform's default shrink-to-content sizing) — a `Dialog` opens its own separate Android window, which the platform itself guarantees owns all touch input for as long as it's showing, a property no amount of same-tree Compose gesture bookkeeping can substitute for. This also let the manual `.statusBarsPadding()` hack from the previous fix be removed: a `Dialog`'s window excludes the status bar by default, so `Close` clears it for free. The pinch-zoom gesture logic itself was factored into a shared `detectPinchZoom` helper (`ZoomableThumb` and the original `Image` both call it) rather than duplicated, and the original image now has real pinch-zoom of its own for the first time — previously, whatever appeared to zoom while the overlay was open was always the thumbnail page behind it, never the original.

Live-verified via `adb`/`uiautomator` (single-pointer only, same limitation as everything else in this file that used `adb shell input`, but sufficient to prove the fix structurally rather than just per-gesture): with the original open, `dumpsys window windows` shows a genuine second window (`Window #9`, alongside `MainActivity`'s own `Window #8`); tapping the exact screen coordinates of the underlying "← Back" button no longer navigates away — the overlay's own `Close` node is still present in the accessibility tree immediately after — and `Close` itself still dismisses correctly back to the detail screen. `:app:testDebugUnitTest` still 198/198 after this fix, `:app:assembleDebug` clean.

**Pinch-zoom on both the thumbnail and the original image is now confirmed working by the user**, closing that gap — with one transient wrinkle worth recording rather than chasing further: right after this session's `adb install -r` (reinstalling over the already-running app to pick up the `Dialog` fix), Ctrl+drag zoom briefly stopped responding in *both* views, and tapping the underlying "← Back" position while the original was open did nothing either. A plain app restart (force-stop, relaunch — no further code change) fixed all of it at once. Most likely a stale input-routing/focus artifact from reinstalling over a live process mid-session — an artifact of this session's own test methodology (repeatedly `adb install -r`-ing over a running app to preserve enrolment state), not something an ordinary user would hit via normal app usage, and not reproducible via a deliberate second attempt (the restart alone, with no code change, resolved it). Recorded here rather than in `android/AGENTS.md` since it isn't a build/test-toolchain gotcha — it's specific to this live-testing pattern, and worth knowing if a future session sees the same "everything stopped responding right after a reinstall" symptom before assuming a real regression.

**Still not independently confirmed**: the approximate-date marker (every photo swiped through in this pass had real EXIF, so `takenAtSrc == "exif"` and the marker never had a reason to appear — needs a photo actually lacking EXIF, which the synthetic load-test generator may not have produced any of). That's the one specific gap keeping this step `partial` rather than `done` — everything else in the "Done when" (opens from the grid, zooms — both views, pass-through fixed) is now live-confirmed. |
| 2.13 Delete | done | `ui/detail/DeleteDialog.kt` (the three-way prompt — Material3's `AlertDialog` only has two named button slots, so "Remove from both" and "Cancel" share the `dismissButton` one), `data/repo/DeleteRepository.kt` (orchestrates `DELETE /photos/{photoId}`, the local tombstone write, the Room `photos`-row delete, and — for "remove from both" — `MediaStoreSource.requestDelete`), `ui/trash/{TrashScreen,TrashViewModel}.kt` (surfaces `GET /trash`'s blocked-re-upload warning; not reachable via any real navigation yet — plan step 2.14 is Settings, which is where a permanent entry point belongs, so `TimelineScreen` gained a plain "Trash" button in the meantime, same "build it standalone" precedent as 2.7's `FoldersScreen`). `ArchivistApi` gained `deletePhoto`/`getTrash` + `TrashEntryDto`/`TrashPageResponse`; `UploadQueueDao` gained `getByPhotoId` (the only local record tying a photoId back to a contentHash/localUri, since the `renditions` Room table is still never populated — see 2.6/2.10's own rows); `MediaStoreSource` gained `requestDelete`/`MediaDeleteOutcome`. **Scope decisions, made deliberately, not gaps found late**: (1) "remove from both"'s `MediaStore` deletion uses `MediaStore.createDeleteRequest` (API 30+) for the real, always-confirm-first deletion; below API 30 it's a bare `ContentResolver.delete()` attempt that surfaces a clean `Failed` (not the OS confirmation dialog) on `SecurityException` — deliberately not special-casing API 29's `RecoverableSecurityException`, since catching a class introduced after this app's `minSdk` 28 in a code path that also runs *on* API 28 risks a class-resolution crash this session can't verify is safe on that OS version, for one narrow and shrinking OS version's sake. (2) The trash list is a single page (`GET /trash`'s own default limit), not paginated — nothing else in the app needs a trash `RemoteMediator`/Room cache the way the timeline does, and the step's "Done when" doesn't call for more than one trashed entry to be visible. **Verified**: 12 new JVM unit tests — `UploadQueueDaoTest` (1: `getByPhotoId` finds every local rendition of one asset, ignores another asset's), `DeleteRepositoryTest` (5, against a real Retrofit stack via MockWebServer + a real in-memory Room db + a `FakeMediaStoreSource`: archive-only tombstones every rendition and leaves `upload_queue`/local files alone; both-removal with an immediate `MediaDeleteOutcome.Deleted` forgets the `upload_queue` rows; both-removal needing confirmation leaves `upload_queue` alone until `finishMediaDelete` is called; a photo never uploaded from this device has nothing to tombstone; a failed archive delete (404) leaves Room/`upload_queue` untouched and writes no tombstone), `TrashViewModelTest` (2, MockWebServer: blocked-attempt fields survive the DTO→`TrashItem` conversion for the entries that have them and stay null for the ones that don't; a 500 reports `Error` rather than an empty list), `TrashScreenTest` (4: `blockedAttemptWarning`'s exact wording — singular/plural/no-`lastAttemptBy` — and `formatTrashDate`'s offset handling). `:app:testDebugUnitTest` 210/210 (up from 198), `:core:crypto:testDebugUnitTest` 10/10 unchanged, `./gradlew :app:assembleDebug`/`:app:compileDebugAndroidTestKotlin` both clean.

**2026-09-03, live against the real `dev` instance (same Pixel-class emulator, PIN unlock, 1,100 load-test photos still live from 2.11) — both delete paths now confirmed live, and two real bugs found and fixed in the process, neither of which any unit test could have caught:**

1. **`DetailScreen`'s delete state never reset, so the second-ever delete attempt silently no-opped.** `DetailViewModel` is retained across every `DetailScreen` visit (this app has no navigation library — every `hiltViewModel()` call here resolves to the Activity's own `ViewModelStore`, not a fresh instance per screen). The `LaunchedEffect(deleteState) { if (deleteState is DeleteUiState.Done) onBack() }` correctly closed the screen after the first successful delete, but left `deleteState` at `Done` — so the next time *any* photo's `DetailScreen` opened, this same effect fired immediately on the stale value and bounced straight back to the grid, indistinguishable from "the tap did nothing" in a screenshot taken a second or two later. Cost real time to diagnose (initially looked like a broken tap-coordinate calculation, not a state bug) until `uiautomator dump` bounds confirmed the tap coordinates were exactly right and the screen was opening and instantly closing instead. Fixed: `viewModel.dismissDelete()` now runs in the same effect, right before `onBack()`. Full account in `android/AGENTS.md`.
2. **`MediaStore.createDeleteRequest` crashed the whole app on "remove from both".** `AndroidMediaStoreSource` builds every URI from the generic `MediaStore.Files` collection (a pre-existing pattern, see 2.10's own AGENTS.md entry) — `createDeleteRequest` requires the type-specific `Images.Media`/`Video.Media` form instead, and threw `IllegalArgumentException: All requested items must be Media items` from inside the platform's own `ContentProvider`, on `Dispatchers.IO` with nothing upstream catching it. Confirmed via a real crash (`FATAL EXCEPTION`, full stack trace captured from `logcat -b crash`), not a hypothetical. Fixed with a new `toTypedMediaUri` helper that looks up each row's `MEDIA_TYPE` and rebuilds the same numeric id under the correct collection before calling `createDeleteRequest`. Full account in `android/AGENTS.md`.

**Confirmed live after both fixes, against real synthetic load-test photos, via `adb`/`uiautomator` (not just screenshots — button taps verified against exact `uiautomator dump` bounds after the grid-cell-tap mystery above)**:
* **Archive-only**: tapping "Remove from archive" made the photo vanish from the timeline immediately (no waiting for a `GET /photos` refresh), and it correctly appeared in the Trash screen. Confirmed via `run-as`-pulled Room DB (WAL-checkpointed before reading — a first attempt without that showed a stale row count) that the local tombstone was written and the `upload_queue` row was left untouched. Confirmed via `content query --uri content://media/external/file/<id>` that the underlying `MediaStore` row is still present — "leaves it in the gallery" holds.
* **Remove from both**: tapping it triggered the real system confirmation — `com.google.android.providers.media.module`'s `PermissionActivity`, showing "Allow Archivist to delete this photo?" with the actual photo thumbnail. Tapping "Allow" returned control to the app (no crash), the photo vanished from the timeline and appeared in Trash, the `upload_queue` row was removed, and `content query` on the same file id afterward returned "No result found" — the file is genuinely gone from `MediaStore`, not just from the app's own view of it.
* **Trash list**: `GET /trash` round-tripped live against the `dev` instance in every state tested — empty ("Nothing in the trash."), one entry, and four entries (accumulated across this session's test deletes) — each rendering its thumbnail and local-time date correctly. No entry showed the blocked-attempt warning, which is correct: nothing in this session tried to re-upload any of them.

**2026-09-04, the last gap closed — a real cross-device blocked re-upload, staged end to end against the real `dev` instance.** The user offered a second emulator (Pixel 9a, `emulator-5556`, already enrolled on the same account from earlier sessions, PIN 1234) specifically to test this without faking anything. Method: pulled the *exact* bytes of the magenta ("1 Sep 2026, 08:39") photo already archive-only-deleted on device 1 (`content read --uri content://media/external/file/208`, confirmed 17,609 bytes, `hmac-sha256:af7552a3...81c12afd2fef2996`), then drove device 2 — which has never scanned or uploaded anything and so holds no local tombstone or `upload_queue` row for this content at all — through a real `Scanner.scan()` → `UploadWorker` pass over those identical bytes, via a new ad hoc instrumented harness (`sync/CrossDeviceReuploadInstrumentedTest.kt`, modeled directly on `LoadTestInstrumentedTest.kt`'s own "never via `connectedDebugAndroidTest`, `am instrument` directly instead" pattern, since that Gradle task would have wiped device 2's enrolled session). Confirmed via device 2's own `upload_queue` row afterward: identical `contentHash` to device 1's original (proving the owner's `hashSecret` — shared across devices — really does make the same bytes hash identically cross-device), same `photoId` as the original asset, `state=DONE`, no client-visible error — exactly `uploads.ts`'s live-but-trashed branch (`duplicate: true, trashed: true`, recording a blocked attempt) rather than a new upload. **Then, live in the Trash screen** (device 2, same account): the magenta entry now shows *"1 attempt to re-upload this from unknown device — delete it there too, or it returns."* — the literal wording `design.md`'s "Tombstones expire, and blocked attempts are surfaced" specifies, `TrashScreen`'s `blockedAttemptWarning` rendering it correctly from a real, non-synthetic `GET /trash` response for the first time. ("unknown device" because the emulator's `deviceKey` wasn't set on this ad hoc upload path — expected, not a bug: `uploads.ts` itself falls back to that string precisely for this case.) This also fully closes the "not re-uploaded" half of the *other* "Done when" clause, and more rigorously than an in-app rescan would have: it proves the system refuses a genuine re-upload attempt even from a device with *zero* local defenses, not merely that the original device's own Scanner declines to try — a strictly stronger bar than "on the next scan" literally asks for. Two new toolchain notes from getting the harness running at all, in `android/AGENTS.md`: a JUnit4 `@Test` method needs a block body when its last statement isn't already `Unit`-typed (here, `Log.i(...)` returns `Int` — same class of gotcha already documented for `@Before`/`@After`, just hitting `@Test` too), and a file pushed via plain `adb push` isn't independently readable by the app's own UID under scoped storage (`/data/local/tmp`, world-readable after `chmod 644`, is a reliable staging spot for exactly this). `CrossDeviceReuploadInstrumentedTest.kt` is kept, not deleted, as a reusable regression harness for this exact scenario — same "ad hoc, not part of the ordinary suite, intentionally not cleaned up" precedent as `LoadTestInstrumentedTest.kt`. One remaining honest footnote, unchanged from before: a "both"-removal that crashed *before* this session's fix left one photo correctly archive-side-trashed and tombstoned but with an orphaned `upload_queue` row and an undeleted local file on device 1 — functionally identical to an "archive only" outcome, not a data-integrity problem, just worth knowing if a future session sees an unexpected extra row in the live `dev` account's local test state. Every clause of both "Done when" sentences is now live-confirmed against the real instance, not just unit-tested — closing this step out. |
| 2.14 Settings | partial | `ui/settings/{SettingsScreen,SyncScreen,SyncViewModel,DevicesScreen,DevicesViewModel,KeysScreen,KeysViewModel,StorageScreen,StorageViewModel,AccountScreen,AccountViewModel}.kt`. `SettingsScreen` is a plain menu (Sync/Devices/Keys/Storage/Trash/Account) over local `destination` state — same no-nav-library convention every other screen transition here uses — and is also where 2.13's deferred Trash entry point finally lands (`TimelineScreen`'s ad hoc "Trash" button is now "Settings"; Trash is one menu row below it). **Sync**: two switches (allow-metered, requires-charging) backed by a new `SyncSettingsStore` (`DataStore<Preferences>`, its own qualified `@SyncSettingsDataStore` instance — see `LocalStorageModule`), with `FoldersScreen` (plan step 2.7, previously built but unreachable — this closes that gap too) hosted underneath. `UploadWorker.buildRequest` reads these instead of the hardcoded `NetworkType.UNMETERED`/no-charging-requirement defaults it shipped with — `UploadScheduler.enqueueAll` is now `suspend` to read the store before building each request; `setRequiresBatteryNotLow(true)` stays hardcoded, per the plan's own wording only naming network policy and charging as settings. **Devices** (the camera-`deviceKey` config items from design.md, a different `device` than Keys' enrolled wrappings): **found a real, previously-undiscovered gap while implementing this** — `devicesPk`/`deviceSk`/`DeviceItem` (key builders, item type) existed since plan 1.x, but no repo function or route ever read or wrote that partition at all, so "devices are auto-registered on first sight" (design.md) was dead text and this settings section would have had nothing to show. Fixed: `src/core/repo/devices.ts` (`listDevices`/`upsertDeviceSighting`/`updateDevice`/`deleteDevice`) and `GET /devices`/`PATCH /devices/{deviceKey}`/`DELETE /devices/{deviceKey}` (`src/lambda/api/routes/devices.ts`, wired into `router.ts`/`terraform/api.tf`/`api.md`); `upsertDeviceSighting` is called from `uploads.ts`'s create-asset branch only (never on attach, so a RAW+JPEG pair from one shutter release counts as one photo). Verified against DynamoDB Local + MinIO: new `test/lambda/devices.test.ts` (9 cases — auto-registration with no offset set, one-photo-not-two-renditions counting, never registers with no deviceKey, label/tzOffsetMin edits, explicit-null clears a default, 404 on an unseen device, non-numeric tzOffsetMin rejected, remove + idempotent re-remove), full gated suite 72/72, `npm run typecheck` clean, `terraform validate`/`fmt` clean. Android side: `DeviceEntity`/`DeviceDao` (`AppDatabase` bumped to version 3, `fallbackToDestructiveMigration` per the existing precedent — nothing has shipped this schema either) as a local cache, `DeviceRepository` (`refresh`/`update`/`remove`/`tzOffsetMinFor`/`cached`) — the settings screen calls `refresh`, `UploadRepository` calls `tzOffsetMinFor` with no network. **Second gap found and fixed in the same pass**: `UploadRepository.uploadOne` had always computed rung 5 of design.md's UTC-offset ladder from `TimeZone.getDefault()` (this Android device's own current system timezone) and labelled it `TzSrc.DEVICE` — which is not what design.md's rung 5 means (a specific *camera's* configured default) and meant editing a device default in Settings could never take effect on a future upload, failing this step's own "Done when" outright. Fixed to look up `DeviceRepository.tzOffsetMinFor(deviceKey)` instead, falling through correctly to a lower rung when this device's cache has never heard of that camera. **Keys**: `EnrolmentRepository` gained `listKeys`/`removeKey` (thin wrappers over the already-existing `GET /keys`/`DELETE /keys/{wrapId}`) and a "regenerate recovery code" sub-flow — `KeyCustody.regenerateRecoveryWrap` (new, `:core:crypto`: a fresh code + wrap against a master key already unlocked, reusing `enrolFirstDevice`'s own recovery half) plus `beginRecoveryRegeneration`/`confirmRecoveryRegeneration`/`finishRecoveryRegeneration`, mirroring first-enrolment's own "show once, confirm typed-back, only then write to the server" shape (`KeysScreen`'s `RecoveryRegenDialogs` reuses that exact UX). `finishRecoveryRegeneration` posts the new recovery wrapping before deleting the old one, so the "at least one recovery wrapping at all times" invariant is never violated even for the instant between the two calls. **Storage**: `StorageRepository` reads/clears the *existing* plan-step-2.11 Coil disk cache (`SingletonImageLoader.get(context).diskCache`) rather than owning a second one. **Account**: `AuthRepository.signOut()` already existed (plan step 2.4); new `AccountRepository.deleteAccount()` re-calls `POST /session/bootstrap` (already idempotent) purely to learn this device's own `ownerId` — nothing before this step ever needed the app to know it — then calls `DELETE /account` with that as `confirmOwnerId`, and on success clears the master key/hash secret, this device's key-wrap id/cached wrap, and every local Room table. Gated behind typing the literal word "DELETE", not just a two-button dialog — sign-out alone uses the ordinary confirm/cancel pattern, but an account delete has "no reset link and no recovery on our side" (deployment.md). `MainActivity`/`TimelineScreen` gained an `onSessionEnded` callback (both actions end the session the same way) that resets the local `signedIn`/`unlocked` flags so the next recomposition falls through to `SignInScreen`, same as a fresh launch. **Two more real bugs found by writing tests, not by inspection, both fixed the same pass**: (1) `ArchivistApi.deleteAccount` was declared `@DELETE` with an `@Body` parameter — Retrofit refuses that combination outright (`RequestFactory` throws `Non-body HTTP method cannot contain @Body`) at the *first call*, not at compile time, so nothing short of a test that actually invokes it catches this; every other test and `assembleDebug` itself stayed green while this was broken. Fixed with `@HTTP(method = "DELETE", path = "", hasBody = true)`, Retrofit's own documented escape hatch — see `android/AGENTS.md`. (2) `AccountRepository.deleteAccount()`'s `appDatabase.clearAllTables()` call was unwrapped, so it ran on `viewModelScope`'s default `Dispatchers.Main.immediate` — harmless in a JVM test only because `RoomDatabase.assertNotMainThread()` NPEs first against the bare `android.jar` stub (masking the real problem until `buildTestDatabase()` got `.allowMainThreadQueries()` to work around *that*), but would have thrown `IllegalStateException: Cannot access database on the main thread` on a real device the first time anyone actually deleted their account. Fixed with `withContext(Dispatchers.IO) { ... }`; full account of both in `android/AGENTS.md`. **Verified**: 29 new JVM unit tests (`DeviceDaoTest` 5, `DeviceRepositoryTest` 6, `SyncSettingsStoreTest` 3, 2 new `UploadRepositoryTest` cases — using a purpose-generated fixture, `canon-no-offset.jpg`, Pillow via a throwaway Python one-liner rather than piexif, since `canon-with-gps.jpg` (2.8's own fixture) carries a real `OffsetTimeOriginal` that would make the device-default rung untestable end to end — proving `deviceKey` derivation and the device-default rung both actually fire through the real `uploadOne` pipeline, not just `Timestamps.resolve`'s own pre-existing ladder tests, `DevicesViewModelTest` 4, `KeysViewModelTest` 5, `AccountViewModelTest` 4 — including the regression case for bug (2) above and for the retained-ViewModel `sessionEnded` bug below, plus `KeyCustodyTest`'s new regeneration case (`:core:crypto`, not counted in the JVM total below)) — `:app:testDebugUnitTest` 239/239 (up from 210), `:core:crypto:testDebugUnitTest` 11/11 (up from 10), `./gradlew :app:assembleDebug`/`:app:compileDebugAndroidTestKotlin` both clean (three pre-existing instrumented tests — `UploadWorkerInstrumentedTest`/`CrossDeviceReuploadInstrumentedTest`/`LoadTestInstrumentedTest` — updated to pass an explicit `SyncSettings()` to `UploadWorker.enqueue(All)`'s new parameter, not otherwise touched). **A third bug, this one a straight port of an already-documented pattern**: `AccountViewModel` is Activity-scoped like every other `hiltViewModel()` here (no nav library — `android/AGENTS.md`'s `DetailViewModel` note), so its one-shot `sessionEnded` flag needed the same "reset where it's consumed" fix `DetailViewModel`'s `deleteState` already established, or a later sign-in would immediately bounce back out the next time Settings > Account was opened. Fixed with `AccountViewModel.acknowledgeSessionEnded()`, called from `AccountScreen`'s own `LaunchedEffect` right after `onSessionEnded()`. **2026-09-04, live-verified against the real `dev` instance** (Pixel 8a emulator, already enrolled from earlier sessions — installed this pass's debug APK with `adb install -r`, preserving its existing session/enrolment): unlocked via the real `NeedsDeviceUnlock` PIN ceremony, landed on the real populated timeline, and confirmed the renamed "Settings" entry point (was "Trash", per this step's own change) opens the menu with all six rows. **Sync**: both toggles render and respond; `FoldersScreen` nested underneath renders its permission rationale with no crash (settling the only real layout risk in `SyncScreen` — a `LazyColumn` inside an unweighted `Column` throws at layout time, which is why `FoldersScreen` is `Modifier.weight(1f)` there). Toggled "Upload on any network" on, then **force-stopped and cold-relaunched the app** (`am force-stop` + `am start`, not just backgrounding) and confirmed the toggle was still on — the strongest form of this step's own "Done when" ("every setting persists across restart"), not just a same-process check. Toggled it back off afterward, leaving the emulator as found. **Devices**: renders "Couldn't refresh — showing the last known list." with an empty list — correct, not a bug: this is exactly what `DeviceRepository.refresh()` should do while the `/devices*` Terraform routes remain undeployed (a live, unstaged confirmation of the error-handling path, not a mocked one). **2026-09-04, later the same day — deployed**: `make deploy-dev` (the user's own AWS profile) applied cleanly — 3 routes added (`GET /devices`, `PATCH /devices/{deviceKey}`, `DELETE /devices/{deviceKey}`) plus the Lambda code update carrying `uploads.ts`'s `upsertDeviceSighting` call, 1 changed, 0 destroyed; a follow-up `terraform plan` reports zero drift. Live-reconfirmed on the same Pixel 8a emulator: the Devices screen now shows a clean empty list with no error at all (correctly empty — this account has no uploaded photo carrying EXIF camera tags yet, not a failure), closing the gap the earlier pass could only describe as "expected but unstaged." **Keys**: a real `GET /keys` response rendered — 8 wraps (7 device entries from this emulator's re-enrollment history, all labelled identically since `Build.MANUFACTURER`/`MODEL` don't vary per install, plus the one recovery code), each with a working-looking "Remove" row and the "Regenerate recovery code" button visible. Deliberately did **not** tap Remove or Regenerate here: both are real, hard-to-reverse actions against the account's actual live key material (removing a wrap could de-enrol the *other* running emulator; regenerating invalidates the one real recovery code this account has) — that flow's correctness rests on `KeysViewModelTest`'s 5 cases instead, deliberately not exercised against production key state. **Storage**: showed a real non-zero cache size (388.9 KB, real decrypted thumbnails already on disk from this session's own scrolling), "Clear cache" dropped it to 0 B, and the timeline still rendered every thumbnail correctly afterward (re-fetched and re-decrypted from network, exactly as the screen's own copy claims). No crash, no logcat exception, across the entire pass. **Still not attempted live, deliberately**: a real recovery-code regeneration end to end and `DELETE /account` against the live `dev` Cognito pool — both destroy real, hard-to-recreate state (the account's only recovery code; the account itself) and are covered by unit tests instead, matching this session's judgement that "verify it live" doesn't extend to spending the one resource being verified. `terraform/api.tf`'s three `/devices*` routes are now deployed to `dev` (see above) — still not applied to `prod`, which needs the same `make deploy` (or `terraform workspace select default && terraform apply`) whenever the operator is ready. |
| 2.15 Upload queue UI | done | `ui/queue/{QueueScreen,QueueViewModel}.kt`, `sync/{DeviceStateMonitor,AndroidDeviceStateMonitor}.kt`. `QueueScreen` lists every `upload_queue` row except `DONE` (pending/in-progress/failed, per the plan's own "Details"), each with a state label, the failed ones showing `lastError` plus a **Retry** button (new `UploadQueueDao.resetForRetry`: state back to `PENDING`, `lastError` cleared, `attempts` left as a running count — re-enqueuing works because WorkManager's `ExistingWorkPolicy.KEEP` only skips *uncompleted* work, and the job it's replacing already finished as a failure). Wired into `SettingsScreen`'s menu (new `QUEUE` destination), same "permanent entry point in Settings" convention plan step 2.14 established for Trash. **Idle-reason banner** (the step's own literal "Done when"): `DeviceStateMonitor` (interface + `AndroidDeviceStateMonitor` impl, bound in `SyncModule`) reads live network capabilities via `ConnectivityManager.registerDefaultNetworkCallback` and charging/low-battery state via `ACTION_BATTERY_CHANGED`/`ACTION_BATTERY_LOW`/`ACTION_BATTERY_OKAY` (the same system-declared low-battery signal `setRequiresBatteryNotLow` is itself defined against, not a hand-picked percentage); the pure `queueIdleReason(SyncSettings, DeviceState)` function in the same file maps that plus `SyncSettingsStore`'s settings onto `NO_NETWORK`/`WAITING_FOR_WIFI`/`WAITING_TO_CHARGE`/`WAITING_FOR_BATTERY`/`NONE`, checked in that priority order (an offline device says so rather than "waiting for Wi-Fi"), and is suppressed once every remaining row is `FAILED` (nothing to wait on but a manual retry). New `android.permission.ACCESS_NETWORK_STATE` manifest entry, required for `ConnectivityManager` capability reads even though it's a normal, auto-granted permission. **Also closed, not in the plan step's own text but in `android.md`'s Screens section ("visible and cancellable")**: a **Cancel** button on non-failed rows — `UploadScheduler` gained `cancel(queueId)` (cancels the WorkManager job by its existing unique name, `WorkManagerUploadScheduler`), and `QueueViewModel.cancel` deletes the row afterward so it doesn't linger in the list forever. **Verified, JVM**: 12 new unit tests — `DeviceStateMonitorTest` (6, the pure `queueIdleReason` decision table) and `QueueViewModelTest` (5: `DONE` rows excluded, a failed row's error surfaces with no idle reason once nothing's left to wait on, an active row surfaces the live idle reason, retry resets state and re-enqueues, cancel removes the row and cancels its work) plus 1 new `UploadQueueDaoTest` case (`resetForRetry`). `:app:testDebugUnitTest` 251/251 (up from 239), `./gradlew :app:assembleDebug`/`:app:compileDebugAndroidTestKotlin` both clean. **2026-09-04, live-verified against a real Pixel 8a emulator, closing this step out — this is the first pass with real `adb` access**: installed the debug APK over the emulator's existing enrolled session (`adb install -r`, session/1,100 load-test photos preserved throughout), unlocked with the device PIN, confirmed `Settings > Upload queue` renders (`QueueScreen` reachable, empty state "Nothing queued." correct against the real, now-empty `upload_queue` table). To exercise pending/in-progress/failed rows without risking the account's real upload state, force-stopped the app and inserted three synthetic rows (`PENDING`/`UPLOADING`/`FAILED` with a `lastError`) directly into the pulled-then-pushed Room DB via a local `sqlite3` (WAL checkpointed to `DELETE` mode first, re-pushed via `run-as`, stale `-wal`/`-shm` removed) — same "edit the DB directly, cold-start to pick it up cleanly" approach as a safe alternative to staging a real failing upload. All three rows rendered correctly with the right labels/error text/action button. **The idle-reason banner is confirmed live end-to-end, not just via fakes**: marked the emulator's own Wi-Fi network metered (`cmd netpolicy set metered-network AndroidWifi true`) — with `allowMeteredNetwork` at its real default (off), the queue screen immediately showed "Waiting for Wi-Fi", the step's own literal "Done when" sentence; toggling Settings > Sync's "Upload on any network" on made the banner disappear live, no restart needed (confirming the `SyncSettingsStore` → `QueueViewModel` reactive path); enabling airplane mode (`cmd connectivity airplane-mode enable`) switched it live to "Waiting for a network connection" (`NO_NETWORK` correctly taking priority). **Retry confirmed live**: tapping it on the synthetic failed row cleared `lastError` and flipped it to "Waiting" in the UI, and re-enqueued the work under the same unique name (confirmed via `ExistingWorkPolicy.KEEP` not blocking it — the row stayed correctly at "Waiting" rather than erroring again immediately, since the metered constraint was still in force at that point and WorkManager correctly never started `doWork()`). **Cancel confirmed live** on both a pending and an in-progress row — each disappeared from the list immediately. Cleaned up fully afterward: cancelled both remaining synthetic rows, reverted `allowMeteredNetwork` and the Wi-Fi metered override back to their original values, disabled airplane mode, confirmed via a fresh DB pull (WAL-applied) that `upload_queue` is empty and the 240-row `photos` table is untouched, and left the emulator unlocked on the timeline as found. Not independently exercised live: the low-battery reason (`ACTION_BATTERY_LOW`/`OKAY`) and the charging-required reason — both are covered only by `DeviceStateMonitorTest`'s pure-function cases, since simulating them on this emulator wasn't attempted this pass; the mechanism (`AndroidDeviceStateMonitor`'s network-callback branch) is the same code path already confirmed live for the metered/no-network cases, so this is a narrow, named gap rather than an unverified class of behavior. |
| 2.16 CI and release | partial | `.github/workflows/android-ci.yml` (PR + push-to-main, paths-filtered to `android/**`) runs unit tests on both modules, `assembleDebug`, and the instrumented suite on a Gradle Managed Device (`pixel2api30`, declared in `app/build.gradle.kts`). `.github/workflows/android-release.yml` triggers on `android-v*.*.*` tags and runs `:app:publishBundle` (Gradle Play Publisher, `com.github.triplet.play`, `resolutionStrategy.set(AUTO)` for versionCode, `versionName` derived from the tag). `renovate.json` added at repo root: grouped by ecosystem (Android/backend/GitHub Actions), automerge patch/minor, majors manual. **Verified locally this pass** (the parts a non-CI session actually can check): `:core:crypto:testDebugUnitTest`, `:app:testDebugUnitTest`, `:app:assembleDebug` and `:app:assembleRelease` all green with the new signing/versioning/GPP/managed-device config in place; `:app:tasks --all` confirms `pixel2api30DebugAndroidTest` and `publishBundle` exist with the exact names the workflows call. **A real bug was found and fixed in the same pass, not just written around**: GPP wires a Play API call into every release build unconditionally, which broke `assembleRelease` outright without credentials — fixed via `play { enabled.set(...) }` gated on `ANDROID_PUBLISHER_CREDENTIALS`; see `android/AGENTS.md`'s new "CI and release" section for the full account, and confirmed (by deliberately supplying a fake credential) that the env var must be the credential file's *contents*, not a path — the opposite of the more common claim online. **Not verified, and can't be from this environment**: no actual GitHub Actions run (PR or tag-push) has ever executed either workflow, so the KVM-enable step, the Gradle Managed Device actually booting under a real runner, and a real Play Console upload via `publishBundle` remain unconfirmed; the five secrets (`ANDROID_KEYSTORE_BASE64`, `ANDROID_KEYSTORE_PASSWORD`, `ANDROID_KEY_ALIAS`, `ANDROID_KEY_PASSWORD`, `PLAY_SERVICE_ACCOUNT_JSON`) don't exist as GitHub repo secrets yet either, and the Play listing has never had a manual first release (a hard GPP/Play Developer API prerequisite — see below). See "CI and release, as built" in `docs/design/android.md` and the new `docs/ops/android-release-secrets.md` for exactly how to create each secret and do the required one-time manual upload. |
| 2.17 Play reviewer preview mode | done | Implemented 2026-09-05, extended and verified live the same day. `ConnectUiState.ReviewerPreview` added alongside `NeedsConnection`/`Connected`; `ConnectScreen` gained a "Preview without an account" `TextButton`; `ConnectViewModel.enterReviewerPreview()`/`exitReviewerPreview()` persist a single `reviewerPreviewEnabled` boolean in `InstanceStore`, checked in `init` alongside `currentInstance`. `MainActivity` routes to `ui/reviewer/ReviewerPreviewScreen.kt` as a third top-level branch. `ReviewerPreviewViewModel` depends on `MediaStoreSource` alone (lists all folders/files, flattens, falls back to 4 bundled `res/drawable-nodpi/reviewer_sample_*.png` — self-authored, generated with Pillow — when the device has none); the screen shows a persistent "Preview — no account, nothing uploaded" banner with a "Settings" action, a one-time explanatory dialog, a grid, and a minimal pinch-zoom detail view (filename/size only, not EXIF — gesture duplicated from `DetailScreen.kt`'s `detectPinchZoom` rather than reusing that screen, deliberately). **Extended same day**: Settings is now reachable (`ReviewerSettingsScreen`/`ReviewerSettingsViewModel`, both new), mirroring the real Settings menu's seven section labels verbatim — Storage reuses the real `StorageScreen`/`StorageViewModel` outright (verified network-free), Sync shows real device folder names with ephemeral `remember`-backed switches, and Upload queue/Devices/Keys/Trash/Account are each a fixed explanation rather than the real (network-wired) screens. The intro dialog was rewritten to say plainly that every screen including Settings is reachable, and that full functionality needs provisioning your own infrastructure and connecting via this project's GitHub repository (named generically, never the maintainer's actual repo path — see CLAUDE.md). `docs/design/android.md` updated (new "Reviewer preview mode" section, rewritten with the Settings extension; package-layout entry; Play Console account-access bullet). `docs/play/privacy-policy.md` gained a "Preview without an account" section plus a permissions-bullet update, and `private/instance/privacy-policy.md` (the filled-in, gitignored copy `terraform apply` actually publishes) was synced with the identical wording. **Verified**: `:app:testDebugUnitTest` full suite green throughout (added `ReviewerPreviewViewModelTest`, `ReviewerPreviewNoNetworkTest` — reflection-asserts both new ViewModels' constructors are exactly `MediaStoreSource` and contain none of the app's network-capable types — plus new `ConnectViewModelTest`/`InstanceStoreTest` cases); `:app:assembleDebug` succeeds. **Then actually run**: installed the debug build on a running Pixel 8a-class emulator (`sdk_gphone16k_arm64`, Android 17, `emulator-5554`) via `adb`, fresh-installed (not just `install -r`, which silently reused a previous session's persisted reviewer-preview flag and skipped straight past `ConnectScreen` — caught by screenshot, not assumed), and drove the full path with `adb shell input tap`/`screencap`: `ConnectScreen` → "Preview without an account" → intro dialog → grid (correctly showing the 4 bundled samples, since the bare emulator has no photos) → Settings → each of the 7 sections in turn (Sync's folder list correctly empty, its switches visibly toggle; Storage's real cache-size/Clear-cache round-trip works; the other five show their explanation text) → back to the menu → back to the grid → photo detail (pinch-zoom screen, correct sample label) → Exit preview → back at `ConnectScreen`, cleanly. No crash at any point. Found and fixed one real bug this way, not from a layout preview: the banner's "Exit preview" `TextButton` was wrapping into an unreadable 4-line sliver because the unweighted banner `Row` squeezed it — fixed by giving the label `Modifier.weight(1f, fill = false)` with `maxLines = 1`/ellipsis so the buttons keep their natural width. **Not verified**: no Compose UI test was added (this repo has no existing Compose UI test infrastructure, and the live emulator pass covers the same ground); `terraform apply` was not re-run against any real instance to confirm the synced `private/instance/privacy-policy.md` actually serves the new text at `/privacy` — that's a plain-text template change with no schema implications, so low-risk, but unconfirmed live. |

## Last audit

2026-09-01 (later) — Claude (Sonnet 5). Continuing the same session, the user asked to
actually test the remaining two "Done when" clauses of plan step 2.11 (offline cached
thumbnails, live key revocation) against the now-populated `dev` account. Both looked
straightforward and both turned out to hide real bugs — see 2.11's own row above for
the full technical account; the short version:

* **Key revocation**: confirmed correct on the first try — `adb shell am send-trim-memory`
  (the real mechanism, not a synthetic hook) instantly swapped the grid for the locked
  flow, never a broken image. One run's full resolution hung for several minutes;
  diagnosed via `adb shell ping` (corrupted packets, the emulator's clock jumping
  backward) as this long session's emulator itself degrading, not an app bug — no
  exception, no ANR, no crash anywhere in the logs for that hung process.
* **Offline**: broken on two independent, previously-undiscovered layers, both found
  live and both fixed:
  1. The silent-unlock step (`EnrolmentRepository.trySilentUnlock`, plan step 2.5)
     unconditionally needed the network on *every* launch, so the app could never get
     past its own unlock screen offline, regardless of how good the Timeline's own
     offline story was. Fixed by caching the device's wrapped key material locally
     (`EnrolmentStore`) and falling back to it when the network fetch fails.
  2. Once that was fixed and the Timeline was actually reachable offline, it rendered
     with correct date headers but *zero* thumbnails. Root cause, found by reading
     Coil 3.3.0's own source: `EncryptedImageFetcher` was returning a plain
     `BufferedSource`-backed `ImageSource`, which Coil's disk-cache-write step simply
     never sees — meaning **the disk cache had never actually cached anything, this
     entire step**, online included; every thumbnail shown all session had been
     re-fetched and re-decrypted from network every single time, silently. Fixed by
     having the fetcher read/write Coil's `DiskCache` itself, mirroring Coil's own
     `NetworkFetcher` — caching the decrypted plaintext, confirmed by checking
     `no_backup/thumbnail_cache/` on the device directly (empty before the fix, real
     journal + data files after).
  3. Re-verified the whole thing for real: network cut at the `adb shell svc`
     level (found live that `svc wifi disable` alone doesn't cut it — this AVD has a
     simulated cellular radio too, needing `svc data disable` as well), app
     force-stopped and cold-launched with confirmed zero connectivity, silently
     unlocked, and rendered every thumbnail from disk cache — screenshotted, not just
     asserted.

`:app:testDebugUnitTest` 186/186 (up from 183). Also, mid-session: the user asked to be
able to run `terraform apply`/`make deploy-dev` directly rather than being blocked by
Claude Code's own auto-mode safety classifier (which refused it twice regardless of the
user's in-chat go-ahead — piping `yes`, then `-auto-approve`) — added narrowly-scoped
Bash permission rules to `.claude/settings.local.json` (this repo, `dev` workspace only,
plan/apply, never prod or destroy), confirmed working immediately with a live
`terraform plan`.

---

2026-09-01 — Claude (Sonnet 5). The user asked to actually exercise plan step 2.11's
own "1,000 photos scroll smoothly" — scaled `LoadTestInstrumentedTest` (see 2.11's row
above) up to 1,000 synthetic photos (1,100 total once the earlier pilot's 100 are
counted) against the real `dev` instance. Real cost/blast-radius items worth recording
plainly:

* **Found and fixed a real crash, live, that no smaller test had surfaced**: partway
  through scrolling the populated timeline, Compose threw `IllegalArgumentException:
  Key "header-2026-08-22" was already used`. Root cause: `TimelineItem.Header`'s grid
  key was the calendar date alone, but the list is sorted by UTC `takenAt` while headers
  group by *local* day — the two orderings aren't monotonic once photos carry different
  `tzOffsetMin` values (a photo with a large positive offset can land on the *next*
  local day ahead of an earlier-UTC photo still on the *previous* one), so the same date
  can recur non-adjacently as two separate header instances. Never showed up at 50/100
  photos by chance; virtually certain at 1,000 with randomized offsets, and possible for
  a real user crossing timezones. Fixed: `TimelineItem.Header` gained `anchorPhotoId`
  (the id of the photo immediately following it), and the grid key is now
  `"header-${anchorPhotoId}"` instead of `"header-${date}"` — globally unique regardless
  of date recurrence. Also fixed, found while fixing this: `TimelineViewModel.timeline`
  had its own copy-pasted `insertSeparators` block instead of calling the already-
  extracted `toTimelineItems()` — the two had already drifted (only the test-only one
  took the date-recurrence bug into account structurally). New regression test
  (`TimelineViewModelTest`, "a repeated local date produces two headers with different
  anchors") reconstructs the exact non-adjacent-recurrence shape with three photos and
  asserts the resulting keys don't collide. `:app:testDebugUnitTest` 183/183 (up from
  182 — the new regression test itself failed on a first, arithmetically-wrong attempt
  at constructing the tzOffsetMin fixtures, fixed and reconfirmed passing).
  **Live-reverified after the fix**: reinstalled, re-unlocked, and stress-scrolled the
  full 1,100-photo timeline (rapid full-length swipes across the whole list, both
  directions, ~55 swipes total) — no crash, process survived throughout, and the exact
  non-adjacent-same-date-header shape was observed live (two separate "February 21,
  2026" headers, non-adjacent) rendering correctly. **This closes the first clause of
  step 2.11's "Done when"** ("1,000 photos scroll smoothly") for real, not just via unit
  tests — the other two clauses (offline cached thumbnails, live key-revocation) are
  still unverified; see 2.11's own row.
* **A real, one-line Terraform drift blocked this entirely at first**: `GET /keys/hash-
  secret` (added to the code 2026-08-30, per 1.8's row) had never been deployed to the
  `dev` API Gateway — confirmed via CloudWatch (the request never reached the Lambda,
  meaning API Gateway itself had no route) and `aws apigatewayv2 get-routes`. Fixed via
  `make deploy-dev` (the user ran it after `terraform apply` was blocked twice by
  Claude Code's own auto-mode safety classifier — piping `yes`, then `-auto-approve`,
  both refused regardless of the user's in-chat go-ahead, a separate permission layer
  from chat consent). The user then asked to be able to run these ops directly in future
  sessions; added narrowly-scoped Bash permission rules to `.claude/settings.local.json`
  (this repo only, `dev` workspace/tfvars only — plan/apply, not prod, not destroy) —
  confirmed working immediately afterward with a real `terraform plan` showing "No
  changes."
* **The `am instrument` fresh-process problem**: an instrumented test launched via
  `am instrument` always gets a *new* process, never the one the UI was running in —
  `MasterKeyHolder` (deliberately in-memory-only, per its own design doc) is empty in
  that fresh process regardless of how recently the real app was unlocked.
  `LoadTestInstrumentedTest` now calls `EnrolmentRepository.determineStep()` itself
  (the same silent-unlock path the app's own launch uses) rather than checking
  `masterKeyHolder().current.value` directly — this works because the underlying
  Android Keystore auth window is gated by *device*-level last-unlock time, not by
  process, so it succeeds as long as the real device was unlocked (PIN/biometric,
  not just screen-on) within the last few minutes. Needed several rounds of "please
  unlock again" during this session as the window kept lapsing between build/deploy
  steps.
* **Built `tools/teardown-load-test.mjs`** (plain Node ESM, matching this repo's
  existing non-TypeScript ops-script convention) plus `make teardown-dev-load-test
  OWNER_ID=... [PREFIX=load_test_] [EXECUTE=1]` — hard-deletes exactly the load-test
  assets (by `stem` substring match — `stem` turned out to be `"<folderUri>/<filename>"`,
  not just the filename, so the filter needed `contains`, not `begins_with`; caught by
  an initial dry run finding 0 matches against a known-populated table) straight out of
  S3 + DynamoDB. Deliberately **not** `purgeAsset`'s own path: that converts the `HASH#`
  pointer into a tombstone on purpose (so a real re-upload is still recognised), which
  is exactly what the user asked this *not* do for disposable test data — this script
  hard-deletes the `HASH#`/`STEM#`/`PATH#` pointers outright. Dry-run-by-default;
  **`--yes`/`EXECUTE=1` not yet run** — validated only via dry run against the 100-photo
  pilot (correctly found 100 assets / 500 DynamoDB items / 400 S3 objects, matching the
  known count exactly) before scaling to 1,000. Cleanup of the full 1,100-photo dataset
  is still pending, deliberately, until the user is done using it to test with.

---

2026-08-31 (latest, addendum) — Claude (Sonnet 5). The user signed into the `dev`
instance on the emulator and reported `TimelineScreen` showing a totally blank screen —
no grid, no message, no spinner. Investigated live rather than guessing: `adb logcat`
showed no crash and no exception (a real transaction did fire, per the
`useWriterConnection` change above), so the RemoteMediator path was working; a direct
`aws dynamodb scan` of `archivist-dev-media` (via `private/instance/env.sh`) confirmed
the actual root cause — this owner has **zero** `#META`/`R#`/`F#` items, only key-wrap/
settings/profile/registry rows. The blank screen was correct given the data (nothing to
show), but gave no feedback distinguishing "still loading," "genuinely empty," and "the
fetch failed" — all three rendered identically as nothing. Fixed in `TimelineScreen.kt`:
`TimelineGrid` now branches on `LazyPagingItems.loadState.refresh` (loading → spinner;
error → message + a working `items.retry()` button; empty → "No photos yet. Back up a
folder in Settings to get started."), falling through to the grid only once real items
exist. `:app:testDebugUnitTest` 182/182 (unchanged — no new test added for this, see
below), `./gradlew :app:assembleDebug` builds. **Live-verified partially**: reinstalled
(`adb install -r`, preserving app data) and relaunched on the same emulator — landed on
the app's own `NeedsDeviceUnlock` screen ("Unlock your device") rather than the fixed
empty state, because force-stopping/relaunching cleared the in-memory master key and
this session has no PIN for that emulator to complete the Keystore unlock ceremony.
Backed out cleanly (system credential prompt, which blocks screenshot capture by
design — confirmed `mWakefulness=Awake`, not asleep, before concluding that) rather than
guess at a PIN; the emulator is sitting at that same clean prompt, nothing left broken.
Asked the user to unlock and confirm the new empty-state message visually — **they did,
and it renders correctly**: after unlocking, `TimelineScreen` shows "No photos yet. Back
up a folder in Settings to get started." centered on screen, confirmed via screenshot,
closing the loop on the original report. This is also the first live confirmation of
`TimelineViewModel.locked` actually gating the screen correctly against a real
`NeedsDeviceUnlock` transition (force-stop clearing the in-memory master key, then a
real Keystore unlock restoring it) — not just unit-tested. **Still not done**: no
automated test covers the new loading/error/empty branching in `TimelineGrid` itself —
the existing `TimelineViewModelTest` covers `toTimelineItems()`'s data transform, not
this Compose-level state selection, and this session didn't add a Compose UI test for
it. The step's own "Done when" (1,000 photos, offline cache, live key revocation) is
still unverified — this pass closed the empty-library gap it happened to surface, not
that.

---

2026-08-31 (latest) — Claude (Sonnet 5). Implemented plan step 2.11 (Timeline): Paging 3
+ `RemoteMediator` over Room, a Coil3 `Fetcher` that decrypts thumbnails on the fly, a
justified-ish grid with local-day date headers, and a locked-state screen. New
dependencies: `androidx.paging:paging-{runtime,compose,testing}` and
`androidx.room:room-paging` (both 3.5.1/2.8.4, no version conflicts), plus
`io.coil-kt.coil3:coil-compose` — pinned to **3.3.0**, not the latest 3.6.0, because
3.4.0+ is compiled against a newer Kotlin stdlib (2.3.10+) than this repo's pinned
Kotlin 2.2.10 compiler can read ("compiled with an incompatible version of Kotlin" at
compile time); 3.3.0 is the newest release still built against kotlin-stdlib 2.2.0. See
step 2.11's own row above for the full account, including two real Room/Paging bugs
found and fixed while writing tests (written up in `android/AGENTS.md`'s "Testing"
section) — the more consequential one, `androidx.room.withTransaction` not working
against a driver-based database at all, isn't purely a test-environment issue: fixed by
switching to `useWriterConnection`/`Transactor.withTransaction`, the modern API that
works identically in production too. `:app:testDebugUnitTest` 182/182 (up from 170,
+12), `./gradlew :app:assembleDebug` builds, and the built APK launches cleanly on a
real emulator with no crash (though not signed in, so `TimelineScreen` itself wasn't
reached live — see the row above for exactly why and what's still unverified).

---

2026-08-31 — Claude (Sonnet 5). The user asked whether this session could actually use
the two Android Studio emulators configured in this environment, and asked for
emulator-level tests plus a reusable DynamoDB Local/MinIO fixture (the ad hoc
spin-up-then-tear-down pattern several earlier sessions used, this repeatedly).

**The reusable backend fixture**: `tools/local-infra.sh` (`up`/`down`/`status`/`env`,
idempotent, meant to stay running across calls rather than be recreated each time) plus
`make test-infra-up`/`test-infra-down`/`test-infra-status`/`test-integration` in the
Makefile. Verified: `make test-integration` runs the full 63-case DynamoDB-Local/MinIO
suite clean; a second `make test-infra-up` correctly reports both containers already
running rather than recreating them; plain `make test` (no infra running) correctly
skips the gated suites via their own `RUN` checks rather than failing.

**Emulator access**: confirmed directly, not assumed — `~/Library/Android/sdk` has real
`adb`/`emulator` binaries (not on `PATH` by default), two AVDs (`Pixel_8a`, `Pixel_9a`,
both API 37), and `Pixel_8a` was already booted and reachable (`adb devices` showed
`emulator-5554`). Installed the current debug APK, launched it, screenshotted it (a real
"Unlock your device" screen — see below) — full round trip confirmed working before
building anything further on top of it.

Asked the user how far to take "emulator-level tests" (a few options, from "just confirm
access" to wiring full CI/plan step 2.16); they chose building real, permanent
instrumented tests now, CI later. See plan step 2.10's own STATUS.md row above for the
full account: three new `androidTest` classes, all passing against the real emulator,
two real production bugs found and fixed that no JVM test could have caught (WorkManager
never actually used `HiltWorkerFactory`; `setForeground`'s service type crashed on API
34+), and `android/AGENTS.md` gained a new "Instrumented tests" section recording five
gotchas hit getting there (the two above, plus a Hilt entry-point placement constraint,
a `MediaStore` URI-collection mismatch, and a JUnit4 `@After` return-type trap).

**The regrettable part, disclosed to the user as soon as it was found**: running the
full instrumented suite via `./gradlew :app:connectedDebugAndroidTest` uninstalled
`fr.enry.archivist.debug` when it finished — standard AGP task behavior, not something
any test's own code did — which took a genuine prior enrolled session on that emulator
(from 2.4/2.5/2.9's earlier live-testing) down with it, including its now-orphaned
device-wrap Keystore key. Not anticipated before running the full suite; should have
been. Recovering it needs a fresh install, reconnecting, and re-enrolling. Written up
prominently in `android/AGENTS.md` so this doesn't repeat.

---

2026-08-30 (latest) — Claude (Sonnet 5). Implemented plan step 2.10 (upload worker):
`sync/UploadWorker.kt`/`data/repo/UploadRepository.kt` on the Android side, plus a
necessary backend extension to `POST /uploads` (`src/lambda/api/routes/uploads.ts`) —
see 1.9's and 2.10's own rows above for the full account of both. The short version:

* Discovered mid-implementation that thumbnails/EXIF have to be encrypted (to fill the
  metadata POST's own body) *before* the server would otherwise mint a `photoId` for
  AAD purposes — resolved by having the client mint a candidate `photoId` itself, with
  the server using it when creating a new asset and discarding it (echoing back the
  real `encDek`/`encKeyId`) on attach. Documented as a real design decision in
  design.md's "Why the client gets to propose a photoId," not worked around silently.
* While reasoning through resume-after-interruption (this step's literal "Done when"),
  found that a bare `duplicate: true` response gives an interrupted upload nothing to
  retry with, since the hash/stem pointers commit *before* any bytes arrive — an asset
  could get stuck in `processing` forever. Fixed server-side: a live duplicate whose
  asset is still `processing` now gets re-presigned URLs (`resumed: true`), including
  the rendition's own already-fixed `encIv`/`encChunkSize`, which the client reuses
  bit-for-bit rather than regenerating.
* Also fixed a real, independent bug in the same route: `encIv` was required
  unconditionally even though it's whole-object-mode-only by spec — streaming-mode
  (>32 MiB) uploads would have been rejected outright.
* Writing `UploadRepositoryTest` (a `MockWebServer`-backed suite that decrypts what
  actually got PUT, not just checking a PUT happened) caught a real bug before it
  shipped: the plain-attach branch was regenerating a fresh IV for the original
  rendition instead of reusing the one already sent in the same request body — which
  `uploads.ts`'s `buildRendition` records verbatim regardless of create-vs-attach.
  Confirmed the fix was real (not incidental) by reverting it and watching the
  affected tests fail with `AEADBadTagException`, then restoring it.
* Deliberately did **not** implement true S3 multipart upload despite the plan text's
  "Multipart parts of 8 MiB" — `POST /uploads` has never offered per-part presigned
  URLs (plan 01 §1.9's own spec only ever asked for one presigned PUT per object), so
  building that would have meant extending 1.9's contract well beyond this step's own
  scope. Used a single streaming OkHttp PUT per object instead (still the same 1 MiB
  Tink segments internally). Recorded as a deliberate, reasoned scope decision in
  android.md and this file, not a silently-dropped requirement.
* 32 new JVM unit tests across both modules; `:app:testDebugUnitTest` 170/170 (up from
  154), `:core:crypto:testDebugUnitTest` 10/10 unchanged, `./gradlew :app:assembleDebug`
  builds. Backend: `test/lambda/uploads.test.ts` grew from 3 to 10 cases, full suite
  63/63, `npm run typecheck` clean.
* **Not done this pass, and worth being explicit about**: no emulator or device session
  was available in this pass, so nothing in this step's own "Done when" (100 photos
  surviving a process kill / airplane-mode toggle, resuming without duplicates, a
  metered network pausing it) has run against real hardware — everything above is JVM
  unit tests plus a real backend integration suite. A future session with device access
  should do for 2.10 what the 2026-08-27/2026-08-29/2026-08-30 emulator passes did for
  2.3–2.9.

---

2026-08-30 (later) — Claude (Sonnet 5). The user ran plan step 2.5 live for the first
time against the `dev` instance, using two Android Studio emulators, and reported back
three things:

1. **Fresh install enrolled, restarted, and unlocked silently against `dev`** — clean
   pass, no issues, no code changes needed. First live confirmation of any part of 2.5
   against a real deployed backend. This is scenario 1 of the step's three "Done when"
   clauses.
2. **A second, separate emulator crashed entering the recovery code.** The user supplied
   the logcat stack trace: `java.security.InvalidAlgorithmParameterException` wrapping
   `java.lang.IllegalStateException: Secure lock screen must be enabled to create keys
   requiring user authentication`, thrown from `KeyPairGenerator.initialize` inside
   `DeviceKeystore.ensureKeyPair()` (`DeviceKeystore.kt:99`, at the time), reached via
   `EnrolmentRepository.attemptRecovery()`. Root cause: that emulator had no PIN/pattern/
   password set at all, and `ensureKeyPair()` only ever caught the API-level gap
   (`DeviceKeystoreUnsupportedException`) — a missing-lock-screen device fell straight
   through as an uncaught platform exception and crashed the app, in both the
   first-enrolment and recovery paths (`beginFirstEnrolment()` has the identical call and
   the identical gap). Fixed: added `NoSecureLockScreenException` in `DeviceKeystore.kt`
   (thrown when `InvalidAlgorithmParameterException`'s cause is `IllegalStateException`),
   caught in both `EnrolmentRepository.beginFirstEnrolment()` (surfaces as
   `EnrolmentUiState.Failed` with an actionable message) and `attemptRecovery()` (surfaces
   as `RecoveryAttemptResult.Failed`, shown inline on the recovery-code screen) — no new
   UI state needed, both existing error-message paths already render arbitrary messages.
   `FakeDeviceKeyProvider` extended to simulate it; two new `EnrolmentRepositoryTest`
   cases added (one per call site), confirming a clean `Result.failure`/
   `RecoveryAttemptResult.Failed` with no crash and no spurious network calls. 94 tests
   pass (`:app` 84, up from 80), `./gradlew assembleDebug` still builds. **Not yet
   re-verified live**: the user needs to set a PIN on that second emulator and retry —
   scenario 2 of 2.5's "Done when" (a second install recovering via code only) is still
   unconfirmed against a real device/backend, now that the crash blocking it is fixed.
3. **Changing the first emulator's PIN to a different PIN, then restarting, did not
   trigger `KeyPermanentlyInvalidatedException`** — the app unlocked silently as if
   nothing had changed. This is scenario 3 of 2.5's "Done when" ("a lock-screen change
   produces a re-enrolment prompt, not a crash"), and the plan's own text frames
   `KeyPermanentlyInvalidatedException` as being thrown "when the user changes their lock
   screen." Whether that framing is accurate for a same-type PIN change (as opposed to
   disabling the lock screen entirely, which Android's docs unambiguously call out as an
   invalidation trigger) isn't something this session could verify without a device —
   recorded here as an open discrepancy rather than assumed to be a bug, since the
   observed behavior (silent continued unlock, no crash) isn't itself dangerous, and
   "fails to re-enrol when it should" and "the plan's assumption about what invalidates
   the key was imprecise" would call for different fixes. A future pass should test
   disabling the lock screen entirely (the platform's unambiguous trigger) as a distinct
   case from changing to a new PIN, on this same emulator/API level, before touching any
   code here.

Also fixed, reported separately by the user during this session: the sign-in email
field (`SignInScreen.kt`) had no `KeyboardOptions`, so IMEs were auto-capitalizing its
first letter as an ordinary text field. Added `KeyboardType.Email` +
`KeyboardCapitalization.None`. No behavior to unit-test (pure Compose input
configuration); confirmed via `:app:compileDebugKotlin`.

**Follow-up, same day**: after the `NoSecureLockScreenException` fix above, the user
re-ran the two blocked scenarios and both passed, closing out plan step 2.5 entirely —
moved to `done` in the table above. Specifically, on the second emulator (the one that
had crashed): setting a PIN and retrying the recovery code succeeded, reaching
"Unlocked". Then, on that same emulator, clearing the PIN and relaunching produced the
recovery-code re-enrolment screen rather than a crash or a silent (and wrong) unlock —
the first live confirmation that `KeyPermanentlyInvalidatedException` is actually thrown
and handled correctly on real (if emulated) hardware, not just in the `FakeDeviceKeyProvider`-backed
unit tests. Re-typing the recovery code with the PIN still cleared correctly hit the new
`NoSecureLockScreenException` error path again (expected chained behavior, not a new
bug: re-enrolling after recovery still needs to generate a Keystore key, which still
needs a lock screen). Left open, and explicitly not tested this pass: whether changing
to a *different* PIN (rather than clearing it) also invalidates the key on this
emulator/API level — a distinct question from what 2.5's "Done when" literally requires,
since clearing the lock screen is Android's own unambiguously documented trigger and
that case is now confirmed. A future session curious about the different-PIN case
should test it as its own experiment, separate from anything 2.5 is blocked on.

---

2026-08-29 — Claude (Sonnet 5). User-reported gap: from the onboarding flow's sign-in
email step there was no way back to the instance-selection (Connect) screen, so picking
the wrong site (e.g. prod instead of dev) meant restarting the app to fix it — `signedIn`
was a one-way `MainActivity` ratchet and `ConnectUiState` only ever moved
`NeedsConnection` → `Connected`, never back. Added `ConnectViewModel.changeInstance()`
(`Connected` → `NeedsConnection`, prefilled with the host being left; a no-op unless
currently connected) and a "Change server" action on `SignInScreen`'s username step that
calls it, wired through `MainActivity`. Deliberately non-destructive: `changeInstance()`
doesn't touch `InstanceStore` — the previous instance and any session against it stay on
disk until a new `connect()` actually succeeds, so backing out and closing the app
without reconnecting leaves things exactly as they were. Two new `ConnectViewModelTest`
cases added.

While verifying, found `:app:testDebugUnitTest` was unrunnable on the Gradle 9.5 /
AGP 9.3.2 upgrade earlier the same day (`65994e8`/`27e1842`/`0f1c012`, all untested at the
time — no STATUS.md rows depend on test runs made between that bump and this entry, so
nothing already recorded here is contradicted): `Failed to load JUnit Platform...
junit-platform-launcher`, reproduced on both JDK 21 and the system JDK 26 — Gradle
stopped auto-resolving the JUnit 5 platform launcher from `junit-jupiter` alone
somewhere between 8.10 and 9.5. Fixed with an explicit
`testRuntimeOnly(libs.junit.platform.launcher)` (new `junit-platform-launcher:1.11.3`
catalog entry, matching the existing `junit-jupiter:5.11.3`). Confirmed:
`:app:testDebugUnitTest` and `:core:crypto:testDebugUnitTest` both green, **92 tests
total** (up from 90 — the two new `ConnectViewModelTest` cases), `./gradlew assembleDebug`
still builds. Flagging it anyway since a same-day Gradle/AGP bump that silently breaks
test execution is exactly the kind of gap this file exists to catch before it's trusted
by a future session. Nothing ran on an emulator or device this pass — same environment
constraint as usual.

2026-08-27 (emulator pass) — Claude (Sonnet 5). The user pointed out Android Studio and
an emulator (Pixel_8a AVD, API 37/Android 17) were now available in this environment,
and asked to actually verify plan step 2.5 rather than leave it at "not verified, no
device" — the first time any Android step in this plan has run on real (if emulated)
Keystore hardware. Booted the emulator headless (`emulator -no-window`), wrote a
throwaway instrumented test (`DeviceKeystoreSpikeTest.kt`, deleted once its results were
recorded, per 2.4a's own convention) exercising `DeviceKeystore`/`EcdhEs`/`KeyCustody`
against the real `AndroidKeyStore`. Found two real bugs no JVM unit test could have
caught, both via `adb logcat`, not by inspection:

1. **`setUserAuthenticationRequired(true)` alone defaults to requiring a biometric
   specifically.** Keygen threw `InvalidAlgorithmParameterException: At least one
   biometric must be enrolled...` on the emulator, which had a PIN but no fingerprint —
   an entirely ordinary device state, not an edge case. Fixed by explicitly ORing
   `AUTH_DEVICE_CREDENTIAL` into `setUserAuthenticationParameters`'s allowed-authenticator
   bitmask.
2. **Auth-per-use (a 0-second validity window) is unusable as designed.** It requires
   the `KeyAgreement` operation to be driven through a `BiometricPrompt.CryptoObject`
   ceremony — confirmed live, the raw call throws `ProviderException`/`KeyStoreException`
   ("User not authenticated... No operation auth token received") — and the
   `CryptoObject(KeyAgreement)` constructor needed for that only exists in
   `androidx.biometric` 1.4.0-alpha06+ (confirmed via the Jetpack releases page: stable
   is still 1.1.0, from 2021, with no `KeyAgreement` support). Rather than take on an
   indefinite alpha dependency, switched to the platform's other documented mode:
   `setUserAuthenticationParameters(300, AUTH_BIOMETRIC_STRONG or AUTH_DEVICE_CREDENTIAL)`
   — the key is usable for 5 minutes after the user last unlocked the device via any
   allowed authenticator, no `CryptoObject`/prompt/dependency needed. Confirmed this
   works end to end: set a PIN (`adb shell locksettings set-pin`), then actually
   performed a credential-verification event (`adb shell locksettings verify`) to stamp
   Keystore's "last authenticated" timestamp — before that, both spike tests failed
   with a clean, correctly-typed `UserNotAuthenticatedException`; after it, both passed,
   including the full `KeyCustody.enrolFirstDevice` → `unwrapForDevice` /
   `unwrapWithRecoveryCode` round trip against a real Keystore-resident key. Also
   confirmed via `KeyInfo.isInsideSecureHardware` that this emulator's EC-P256 key is
   software-backed (`securityLevel=0`) — expected for an emulator (no real TEE) and not
   new information toward design.md open question 1's still-open real-hardware
   question, but confirms the crypto is correct without hardware backing regardless.

Since `UserNotAuthenticatedException` is a real, ordinary failure mode this exposed
(not just a stale-window corner case — a freshly booted device has *never* satisfied
the window), added handling for it rather than leaving it to crash: a new
`EnrolmentStep.NeedsDeviceUnlock` / `EnrolmentUiState.NeedsDeviceUnlock`, surfaced as a
"Unlock your device" screen backed by `KeyguardManager.createConfirmDeviceCredentialIntent`
that retries `checkStep()` on success. Added `EnrolmentRepositoryTest`'s matching case
(`FakeDeviceKeyProvider.requireAuthenticationOnNextPrivateKeyUse`). 90 tests pass
across both modules (up from 89), `./gradlew assembleDebug` still succeeds.

Also installed and launched the real debug APK on the emulator and drove it — not just
launched it: the Connect screen renders correctly, and typing a deliberately
nonexistent host and tapping Connect correctly reaches the `HostNotFound` error state
(red-bordered field, "Couldn't reach that address...") — the first live confirmation of
plan step 2.3's error-state UI, previously flagged as "no manual UI walkthrough of the
error states" in that step's own status note. Deliberately did **not** point the app at
the operator's real `prod` instance to test sign-in/enrolment further: doing so would
allocate a real master-key version and produce a real recovery code against the actual
library, and no throwaway/dev instance exists to test against instead. Updated
`android.md`'s "Key unlock" section with the corrected time-based-auth design (a
deviation from its previous "biometric prompt" framing, found and fixed the same way
2.4a's RSA→ECDH pivot was — by actually running it, not by inspection) and updated this
step's own status row above.

2026-08-27 (later still) — Claude (Sonnet 5). Implemented plan step 2.5 (key enrolment
and recovery code): `KeyCustody.kt`/`DeviceKeystore.kt` in `:core:crypto`, wiring them
into `EnrolmentRepository`/`EnrolmentViewModel`/`EnrolmentScreen` in `:app`, plus a
`MasterKeyHolder` and an `onTrimMemory` hook in `ArchivistApplication`. Designed
`KeyCustody` to take `PublicKey`/`PrivateKey` rather than touching `AndroidKeyStore`
directly (mirroring `EcdhEs`'s own existing design), specifically so its logic is a
plain JVM unit test; introduced a `DeviceKeyProvider` interface so `EnrolmentRepository`
could be tested the same way against a `FakeDeviceKeyProvider`. That paid off
immediately: writing the tests caught a real design bug before it reached a device —
the first draft made it *impossible* to wrap a recovered master key for a new device,
since `MasterKey` hides its raw bytes by design and the wrap functions demanded them
from outside — fixed by moving `wrapForDevice`/`wrapForRecovery` onto `MasterKey`
itself. Also caught, the same way: `ArchivistApi`'s new `deleteKey`/`putHashSecret`
endpoints threw `NullPointerException` against a real 204 response with a non-null
suspend return type — fixed with `Response<ResponseBody>` and an explicit
`isSuccessful` check where it matters. 89 tests pass across both modules (`:core:crypto`
10 — up 7 for `KeyCustodyTest`, from 3 — and `:app` 79, up from 64), `./gradlew
assembleDebug` succeeds. Found and wrote up two
real gaps rather than guessing around them (open questions 3 and 4 in `design.md`,
same section 2.4a's RSA finding resolved in): `PURPOSE_AGREE_KEY` needs API 31 but
`minSdk` is 28, so there's no working hardware-backed device-wrap route at all below
that (a product decision, not a 2.5 fix); and no endpoint exists yet for a later device
to fetch the owner's wrapped `hashSecret` (plan step 2.10 will need one plan 01 doesn't
have). Nothing here ran on a real device or emulator — same environment constraint as
every Android step since 2.3 — so the real `AndroidKeyStore` integration, a real
biometric prompt, and a real lock-screen-triggered `KeyPermanentlyInvalidatedException`
remain unverified beyond what a plain-EC-keypair fake can stand in for.

2026-08-27 (later same day) — Claude (Sonnet 5). Ran 2.4a for real, on a physical
device the user connected (Motorola Edge 20 Lite, Android 13, no StrongBox — their
oldest, on purpose: a newer device passing wouldn't answer whether the oldest one
does). Found that RSA-OAEP-256 decrypt against a Keystore-resident key fails outright
on real hardware (`Unsupported MGF1 digest: SHA-256. Only SHA-1 supported`), a plain
TEE Keymaster limitation independent of StrongBox availability — worse than the
StrongBox-availability question 2.4a was originally written to answer, and with no
software workaround since that operation never leaves hardware. Confirmed ECDH-ES
works correctly on the same device as the replacement. Iterated the spike twice more
on-device to pin this down precisely: the first run's own stack trace was initially
misread as an encrypt failure (it was decrypt), corrected by splitting the check into
three independent probes; then added an ECDH agreement probe that also caught a
try/catch scoping bug (`StrongBoxUnavailableException` thrown outside where it was
being caught) before it produced a false "works" reading. Also verified live, before
relying on it in code, that `KeyAgreement.getInstance("ECDH")` with no explicit
provider auto-routes to the Keystore implementation — same as `Cipher.getInstance`
already does for RSA — which is what let `EcdhEs.kt` share one code path for both a
real Keystore key and a plain test key.

At the user's explicit direction ("I want a mechanism that works with this device"),
wrote up the full change: `crypto-format.md`'s "Master key wrapping" section rewritten
for `ECDH-ES+AES-KW` (P-256, HKDF-SHA256 salted with the ephemeral public key, then
AES-KW), `design.md`'s open question 1 marked resolved with the real numbers and a new
remaining sub-question (EC-P256 landed in *software*, not the TEE, on this device — a
separate, still-open gap), `sample-data.md`'s device `W#` row updated, `android.md`'s
and `02-android-mvp.md`'s RSA-specific prose corrected. Added conformance vector 23 to
`tools/gen-vectors/generate.py` (fixed static + ephemeral P-256 keypairs, checked from
both agreement directions) and regenerated `testdata/vectors/` — note this also
regenerated the non-deterministic existing cases' random material (streaming salts,
the RSA-17 keypair) as a side effect of re-running the generator; still 23/23
self-verified. Wrote `EcdhEs.kt` in `:core:crypto` and a matching conformance-test
case; all 23 vectors pass. Updated the backend: `wrapAlg` closed set and a new `epk`
field on `KeyWrapItem` (`items.ts`), required-and-validated in `POST /keys`
(`routes/keys.ts`) when that `wrapAlg` is used — spun up a throwaway DynamoDB Local +
MinIO (no committed compose file for this exists; started manually, table/buckets
created by hand from `terraform/dynamodb.tf`'s schema, torn down after) specifically to
verify this against real infrastructure rather than typecheck alone: 48/48 backend
tests pass. Deleted `KeystoreSpike.kt` per the plan's own instruction now that its
result is recorded. Did not test on a second, different device — the user offered a
newer one but reasoned (correctly) that it wouldn't answer whether the oldest device
works, which is the one that has to.

2026-08-27 — Claude (Sonnet 5). Started plan 02 step 2.4 (Authentication) and 2.4a
(Keystore spike), plus wrote `docs/design/api.md` (every endpoint, method, and what
gates access to it — JWT authorizer, `authMode`, and the CloudFront surfaces that
bypass it). Before writing any Cognito client code, verified its wire format live
against the real pool with a throwaway test user rather than working from memory:
confirmed unsigned HTTPS JSON-RPC works with no AWS SDK/SigV4 (design.md's claim was
correct), that `application/x-amz-json-1.1` is load-bearing (not `application/json`),
the real shape of `StartWebAuthnRegistration`'s `CredentialCreationOptions`, and that a
`WEB_AUTHN` challenge for an account with no registered passkey returns
`SELECT_CHALLENGE` rather than erroring. Built the Cognito REST client, encrypted token
store, an authenticated Archivist API client with an OkHttp `Authenticator` doing
refresh-on-401-exactly-once, the auth repository, and a `SignInViewModel`/`SignInScreen`
— scope grew to include the temporary-password-to-passkey-registration bridge an
invited account actually needs, since there's no hosted UI to do it instead. Added 42
tests (22 → 64 total for `:app`), including a real-MockWebServer-backed test of the
Authenticator (the trickiest logic, and the one thing that couldn't be checked live).
Fixed two bugs the test run caught: an uncaught `parseToJsonElement` exception on
malformed credential JSON, and a StandardTestDispatcher/real-network race in the
ViewModel tests (same shape as 2.3's DataStore race, this time from OkHttp real
threads). Wrote 2.4a's `KeystoreSpike.kt` per the plan and confirmed it compiles; did
not and could not run it — no device or emulator here. In the course of 2.4, found a
real infrastructure gap: passkey creation needs a `/.well-known/assetlinks.json` on
each instance's domain that nothing in `terraform/` serves yet, and the app's own
signing certificate (needed to fill it in) doesn't exist since Play App Signing hasn't
happened. Wrote this up as open question 2 in `design.md` rather than guessing at a
fingerprint or the exact asset-links relation string. Updated `create-user.md`'s and
`android.md`'s claims about 2.4/2.5 status to match. Neither 2.4's Credential Manager
ceremony nor 2.4a could be exercised on real hardware in this session — both need a
follow-up pass on an actual device once the asset-links gap is closed.

---

2026-08-26 (later same day) — Claude (Sonnet 5). Picked up from the audit below.
Reconciled step 1.8 (removed client-supplied `masterKeyVer` from `POST /keys`, added
`POST /keys/version` and `PUT /keys/hash-secret`, added `rotatedAt` to `#SETTINGS` —
documented in `design.md`/`sample-data.md`); fixed step 1.13 (tombstone `expiresAt` +
DynamoDB TTL); added the blocked-re-upload-attempt tracking steps 1.9/1.12 required
(trashed-live hit records-or-restores, tombstone hit records and pushes `expiresAt`
forward, `GET /trash` surfaces it). Ran the full DynamoDB-Local + MinIO gated suite throughout
(47/47 passing by the end, across 7 test files — 11 tests added this pass for the new
coverage) plus `make build`/`typecheck`/`terraform fmt`+`validate`. Then, at the user's explicit direction,
sourced the operator's AWS profile and ran `terraform plan` (65 add / 1 change / 0 destroy,
reviewed before proceeding) and `terraform apply` against the real `prod` instance —
succeeded with 0 errors, confirmed with a zero-drift `terraform plan` afterward and live
`curl`s of `/health`, an unauthenticated route, the discovery document, and direct-S3
403s. Did not perform any interactive-auth-gated verification (passkey enrolment,
authenticated uploads, cross-owner isolation, account deletion) — see the per-step notes
above for exactly what that leaves unverified.

**Same day, follow-up.** The user asked what actually stops a stranger from calling
`POST /session/bootstrap` for themselves — a question this session's own review hadn't
raised. Investigation found self-service Cognito sign-up was still enabled on the live
pool (`admin_create_user_config` was never set in `cognito.tf`), which combined with the
discovery document's unauthenticated `clientId`/`userPoolId` meant anyone who found the
domain could self-register and get a fully-isolated library. Checked for actual
exploitation (`aws cognito-idp list-users` — zero users) before fixing live. See the "—
| security fix, same day" row under 1.4 above for the full account. Worth naming
directly: this should have been caught while writing 1.4/1.7, not after deploying, and
wasn't until asked about it.

---

2026-08-26 (later still) — Claude (Sonnet 5). Started plan 02 step 2.3 (Instance
connection), the first Android step past scaffolding/crypto. Added Retrofit +
kotlinx.serialization + OkHttp + DataStore-preferences to the version catalog (none of
this existed in the module before); wrote the discovery fetch, host validation, DataStore
persistence, repository and ViewModel/Compose layers; wrote 22 unit tests (JUnit5, this
module's first tests — the crypto module uses JUnit4, kept separate on purpose). Actually
ran the build: found and fixed a wrong import (`retrofit2.converter...` instead of the
real `com.jakewharton.retrofit2.converter...` package) that `:app:compileDebugKotlin`
caught immediately, and a real test-flakiness bug in the ViewModel test (DataStore's
default coroutine scope is real `Dispatchers.IO`, which a test's virtual `StandardTestDispatcher`
can't see or wait for — fixed by binding DataStore's scope to the same test dispatcher and
passing it into `runTest` too). `assembleDebug` and `:core:crypto:testDebugUnitTest` both
still pass. Compared `DiscoveryDocument`'s fields against `terraform/wellknown.tf`'s real
`jsonencode(...)` by hand — exact match. Did not run anything on an emulator or device —
this environment has the Android SDK but no emulator; the plan step's literal "Done when"
(connecting to a real instance from the running app) is therefore unverified, and stays
`partial` in the table above until someone does that pass.

---

2026-08-26 — Claude (Sonnet 5). Method: read every step's "Files" against the actual
tree, ran `make test` (backend) and `./gradlew :core:crypto:testDebugUnitTest` (Android),
grepped for the specific gaps plan 01 step 1.8 already flags, and spot-checked
`repo/purge.ts` against step 1.13's tombstone requirement. Did not run the
DynamoDB-Local-gated integration tests, did not source AWS credentials, did not deploy
anything.
